/*
** 2013-06-12
**
** The author disclaims copyright to this source code.  In place of
** a legal notice, here is a blessing:
**
**    May you do good and not evil.
**    May you find forgiveness for yourself and forgive others.
**    May you share freely, never taking more than you give.
**
*************************************************************************
**
** A shim that sits between the SQLite virtual table interface and
** runtimes with garbage collector based memory management.
*/
#include "sqlite3ext.h"
SQLITE_EXTENSION_INIT1
#include <assert.h>
#include <string.h>

#ifndef SQLITE_OMIT_VIRTUALTABLE

/* Forward references */
typedef struct vtshim_aux vtshim_aux;
typedef struct vtshim_vtab vtshim_vtab;
typedef struct vtshim_cursor vtshim_cursor;


/* The vtshim_aux argument is the auxiliary parameter that is passed
** into sqlite3_create_module_v2().
*/
struct vtshim_aux {
  void *pChildAux;              /* pAux for child virtual tables */
  void (*xChildDestroy)(void*); /* Destructor for pChildAux */
  sqlite3_module *pMod;         /* Methods for child virtual tables */
  sqlite3 *db;                  /* The database to which we are attached */
  char *zName;                  /* Name of the module */
  int bDisposed;                /* True if disposed */
  vtshim_vtab *pAllVtab;        /* List of all vtshim_vtab objects */
  sqlite3_module sSelf;
  int *xChildDestroy_signature;
         /* Methods used by this shim */
};

/* A vtshim virtual table object */
struct vtshim_vtab {
  sqlite3_vtab base;       /* Base class - must be first */
  sqlite3_vtab *pChild;    /* Child virtual table */
  vtshim_aux *pAux;        /* Pointer to vtshim_aux object */
  vtshim_cursor *pAllCur;  /* List of all cursors */
  vtshim_vtab **ppPrev;    /* Previous on list */
  vtshim_vtab *pNext;      /* Next on list */
};

/* A vtshim cursor object */
struct vtshim_cursor {
  sqlite3_vtab_cursor base;    /* Base class - must be first */
  sqlite3_vtab_cursor *pChild; /* Cursor generated by the managed subclass */
  vtshim_cursor **ppPrev;      /* Previous on list of all cursors */
  vtshim_cursor *pNext;        /* Next on list of all cursors */
};

/* Macro used to copy the child vtable error message to outer vtable */
#define VTSHIM_COPY_ERRMSG()                                             \
  do {                                                                   \
    sqlite3_free(pVtab->base.zErrMsg);                                   \
    pVtab->base.zErrMsg = sqlite3_mprintf("%s", pVtab->pChild->zErrMsg); \
  } while (0)

/* Methods for the vtshim module */
static int vtshimCreate(
  sqlite3 *db,
  void *ppAux,
  int argc,
  const char *const*argv,
  sqlite3_vtab **ppVtab,
  char **pzErr
){
  vtshim_aux *pAux = (vtshim_aux*)ppAux;
  vtshim_vtab *pNew;
  int rc;

  assert( db==pAux->db );
  if( pAux->bDisposed ){
    if( pzErr ){
      *pzErr = sqlite3_mprintf("virtual table was disposed: \"%s\"",
                               pAux->zName);
    }
    return SQLITE_ERROR;
  }
  pNew = sqlite3_malloc( sizeof(*pNew) );
  *ppVtab = (sqlite3_vtab*)pNew;
  if( pNew==0 ) return SQLITE_NOMEM;
  memset(pNew, 0, sizeof(*pNew));
  if (memcmp(pAux->pMod->xCreate_signature, xCreate_signatures[xCreate_0_enum], sizeof(pAux->pMod->xCreate_signature)) == 0) {
    rc = 0;
  }
  else
    if (memcmp(pAux->pMod->xCreate_signature, xCreate_signatures[xCreate_amatchConnect_enum], sizeof(pAux->pMod->xCreate_signature)) == 0) {
      rc = amatchConnect(db, pAux->pChildAux, argc, argv, &pNew->pChild,
                         pzErr);
    }
  else
    if (memcmp(pAux->pMod->xCreate_signature, xCreate_signatures[xCreate_closureConnect_enum], sizeof(pAux->pMod->xCreate_signature)) == 0) {
      rc = closureConnect(db, pAux->pChildAux, argc, argv, &pNew->pChild,
                          pzErr);
    }
  else
    if (memcmp(pAux->pMod->xCreate_signature, xCreate_signatures[xCreate_csvtabCreate_enum], sizeof(pAux->pMod->xCreate_signature)) == 0) {
      rc = csvtabCreate(db, pAux->pChildAux, argc, argv, &pNew->pChild, pzErr);
    }
  else
    if (memcmp(pAux->pMod->xCreate_signature, xCreate_signatures[xCreate_dbpageConnect_enum], sizeof(pAux->pMod->xCreate_signature)) == 0) {
      rc = dbpageConnect(db, pAux->pChildAux, argc, argv, &pNew->pChild,
                         pzErr);
    }
  else
    if (memcmp(pAux->pMod->xCreate_signature, xCreate_signatures[xCreate_echoCreate_enum], sizeof(pAux->pMod->xCreate_signature)) == 0) {
      rc = echoCreate(db, pAux->pChildAux, argc, argv, &pNew->pChild, pzErr);
    }
  else
    if (memcmp(pAux->pMod->xCreate_signature, xCreate_signatures[xCreate_expertConnect_enum], sizeof(pAux->pMod->xCreate_signature)) == 0) {
      rc = expertConnect(db, pAux->pChildAux, argc, argv, &pNew->pChild,
                         pzErr);
    }
  else
    if (memcmp(pAux->pMod->xCreate_signature, xCreate_signatures[xCreate_f5tOrigintextCreate_enum], sizeof(pAux->pMod->xCreate_signature)) == 0) {
      rc = f5tOrigintextCreate(db, pAux->pChildAux, argc, argv, &pNew->pChild,
                               pzErr);
    }
  else
    if (memcmp(pAux->pMod->xCreate_signature, xCreate_signatures[xCreate_f5tTokenizerCreate_enum], sizeof(pAux->pMod->xCreate_signature)) == 0) {
      rc = f5tTokenizerCreate(db, pAux->pChildAux, argc, argv, &pNew->pChild,
                              pzErr);
    }
  else
    if (memcmp(pAux->pMod->xCreate_signature, xCreate_signatures[xCreate_fsConnect_enum], sizeof(pAux->pMod->xCreate_signature)) == 0) {
      rc = fsConnect(db, pAux->pChildAux, argc, argv, &pNew->pChild, pzErr);
    }
  else
    if (memcmp(pAux->pMod->xCreate_signature, xCreate_signatures[xCreate_fsdirConnect_enum], sizeof(pAux->pMod->xCreate_signature)) == 0) {
      rc = fsdirConnect(db, pAux->pChildAux, argc, argv, &pNew->pChild, pzErr);
    }
  else
    if (memcmp(pAux->pMod->xCreate_signature, xCreate_signatures[xCreate_fstreeConnect_enum], sizeof(pAux->pMod->xCreate_signature)) == 0) {
      rc = fstreeConnect(db, pAux->pChildAux, argc, argv, &pNew->pChild,
                         pzErr);
    }
  else
    if (memcmp(pAux->pMod->xCreate_signature, xCreate_signatures[xCreate_fts3CreateMethod_enum], sizeof(pAux->pMod->xCreate_signature)) == 0) {
      rc = fts3CreateMethod(db, pAux->pChildAux, argc, argv, &pNew->pChild,
                            pzErr);
    }
  else
    if (memcmp(pAux->pMod->xCreate_signature, xCreate_signatures[xCreate_fts3auxConnectMethod_enum], sizeof(pAux->pMod->xCreate_signature)) == 0) {
      rc = fts3auxConnectMethod(db, pAux->pChildAux, argc, argv,
                                &pNew->pChild, pzErr);
    }
  else
    if (memcmp(pAux->pMod->xCreate_signature, xCreate_signatures[xCreate_fts3termConnectMethod_enum], sizeof(pAux->pMod->xCreate_signature)) == 0) {
      rc = fts3termConnectMethod(db, pAux->pChildAux, argc, argv,
                                 &pNew->pChild, pzErr);
    }
  else
    if (memcmp(pAux->pMod->xCreate_signature, xCreate_signatures[xCreate_fts3tokConnectMethod_enum], sizeof(pAux->pMod->xCreate_signature)) == 0) {
      rc = fts3tokConnectMethod(db, pAux->pChildAux, argc, argv,
                                &pNew->pChild, pzErr);
    }
  else
    if (memcmp(pAux->pMod->xCreate_signature, xCreate_signatures[xCreate_fuzzerConnect_enum], sizeof(pAux->pMod->xCreate_signature)) == 0) {
      rc = fuzzerConnect(db, pAux->pChildAux, argc, argv, &pNew->pChild,
                         pzErr);
    }
  else
    if (memcmp(pAux->pMod->xCreate_signature, xCreate_signatures[xCreate_geopolyCreate_enum], sizeof(pAux->pMod->xCreate_signature)) == 0) {
      rc = geopolyCreate(db, pAux->pChildAux, argc, argv, &pNew->pChild,
                         pzErr);
    }
  else
    if (memcmp(pAux->pMod->xCreate_signature, xCreate_signatures[xCreate_intarrayCreate_enum], sizeof(pAux->pMod->xCreate_signature)) == 0) {
      rc = intarrayCreate(db, pAux->pChildAux, argc, argv, &pNew->pChild,
                          pzErr);
    }
  else
    if (memcmp(pAux->pMod->xCreate_signature, xCreate_signatures[xCreate_pcache1Create_enum], sizeof(pAux->pMod->xCreate_signature)) == 0) {
      rc = pcache1Create(db, pAux->pChildAux, argc, argv, &pNew->pChild,
                         pzErr);
    }
  else
    if (memcmp(pAux->pMod->xCreate_signature, xCreate_signatures[xCreate_pcachetraceCreate_enum], sizeof(pAux->pMod->xCreate_signature)) == 0) {
      rc = pcachetraceCreate(db, pAux->pChildAux, argc, argv, &pNew->pChild,
                             pzErr);
    }
  else
    if (memcmp(pAux->pMod->xCreate_signature, xCreate_signatures[xCreate_porterCreate_enum], sizeof(pAux->pMod->xCreate_signature)) == 0) {
      rc = porterCreate(db, pAux->pChildAux, argc, argv, &pNew->pChild, pzErr);
    }
  else
    if (memcmp(pAux->pMod->xCreate_signature, xCreate_signatures[xCreate_rtreeCreate_enum], sizeof(pAux->pMod->xCreate_signature)) == 0) {
      rc = rtreeCreate(db, pAux->pChildAux, argc, argv, &pNew->pChild, pzErr);
    }
  else
    if (memcmp(pAux->pMod->xCreate_signature, xCreate_signatures[xCreate_schemaCreate_enum], sizeof(pAux->pMod->xCreate_signature)) == 0) {
      rc = schemaCreate(db, pAux->pChildAux, argc, argv, &pNew->pChild, pzErr);
    }
  else
    if (memcmp(pAux->pMod->xCreate_signature, xCreate_signatures[xCreate_simpleCreate_enum], sizeof(pAux->pMod->xCreate_signature)) == 0) {
      rc = simpleCreate(db, pAux->pChildAux, argc, argv, &pNew->pChild, pzErr);
    }
  else
    if (memcmp(pAux->pMod->xCreate_signature, xCreate_signatures[xCreate_spellfix1Create_enum], sizeof(pAux->pMod->xCreate_signature)) == 0) {
      rc = spellfix1Create(db, pAux->pChildAux, argc, argv, &pNew->pChild,
                           pzErr);
    }
  else
    if (memcmp(pAux->pMod->xCreate_signature, xCreate_signatures[xCreate_statConnect_enum], sizeof(pAux->pMod->xCreate_signature)) == 0) {
      rc = statConnect(db, pAux->pChildAux, argc, argv, &pNew->pChild, pzErr);
    }
  else
    if (memcmp(pAux->pMod->xCreate_signature, xCreate_signatures[xCreate_tclConnect_enum], sizeof(pAux->pMod->xCreate_signature)) == 0) {
      rc = tclConnect(db, pAux->pChildAux, argc, argv, &pNew->pChild, pzErr);
    }
  else
    if (memcmp(pAux->pMod->xCreate_signature, xCreate_signatures[xCreate_tclvarConnect_enum], sizeof(pAux->pMod->xCreate_signature)) == 0) {
      rc = tclvarConnect(db, pAux->pChildAux, argc, argv, &pNew->pChild,
                         pzErr);
    }
  else
    if (memcmp(pAux->pMod->xCreate_signature, xCreate_signatures[xCreate_unicodeCreate_enum], sizeof(pAux->pMod->xCreate_signature)) == 0) {
      rc = unicodeCreate(db, pAux->pChildAux, argc, argv, &pNew->pChild,
                         pzErr);
    }
  else
    if (memcmp(pAux->pMod->xCreate_signature, xCreate_signatures[xCreate_unionConnect_enum], sizeof(pAux->pMod->xCreate_signature)) == 0) {
      rc = unionConnect(db, pAux->pChildAux, argc, argv, &pNew->pChild, pzErr);
    }
  else
    if (memcmp(pAux->pMod->xCreate_signature, xCreate_signatures[xCreate_vlogConnect_enum], sizeof(pAux->pMod->xCreate_signature)) == 0) {
      rc = vlogConnect(db, pAux->pChildAux, argc, argv, &pNew->pChild, pzErr);
    }
  else
    if (memcmp(pAux->pMod->xCreate_signature, xCreate_signatures[xCreate_vtablogCreate_enum], sizeof(pAux->pMod->xCreate_signature)) == 0) {
      rc = vtablogCreate(db, pAux->pChildAux, argc, argv, &pNew->pChild,
                         pzErr);
    }
  else
    if (memcmp(pAux->pMod->xCreate_signature, xCreate_signatures[xCreate_wholenumberConnect_enum], sizeof(pAux->pMod->xCreate_signature)) == 0) {
      rc = wholenumberConnect(db, pAux->pChildAux, argc, argv, &pNew->pChild,
                              pzErr);
    }
  else
    if (memcmp(pAux->pMod->xCreate_signature, xCreate_signatures[xCreate_zipfileConnect_enum], sizeof(pAux->pMod->xCreate_signature)) == 0) {
      rc = zipfileConnect(db, pAux->pChildAux, argc, argv, &pNew->pChild,
                          pzErr);
    }
  if( rc ){
    sqlite3_free(pNew);
    *ppVtab = 0;
    return rc;
  }
  pNew->pAux = pAux;
  pNew->ppPrev = &pAux->pAllVtab;
  pNew->pNext = pAux->pAllVtab;
  if( pAux->pAllVtab ) pAux->pAllVtab->ppPrev = &pNew->pNext;
  pAux->pAllVtab = pNew;
  return rc;
}

static int vtshimConnect(
  sqlite3 *db,
  void *ppAux,
  int argc,
  const char *const*argv,
  sqlite3_vtab **ppVtab,
  char **pzErr
){
  vtshim_aux *pAux = (vtshim_aux*)ppAux;
  vtshim_vtab *pNew;
  int rc;

  assert( db==pAux->db );
  if( pAux->bDisposed ){
    if( pzErr ){
      *pzErr = sqlite3_mprintf("virtual table was disposed: \"%s\"",
                               pAux->zName);
    }
    return SQLITE_ERROR;
  }
  pNew = sqlite3_malloc( sizeof(*pNew) );
  *ppVtab = (sqlite3_vtab*)pNew;
  if( pNew==0 ) return SQLITE_NOMEM;
  memset(pNew, 0, sizeof(*pNew));
  if (memcmp(pAux->pMod->xConnect_signature, xConnect_signatures[xConnect_amatchConnect_enum], sizeof(pAux->pMod->xConnect_signature)) == 0) {
    rc = amatchConnect(db, pAux->pChildAux, argc, argv, &pNew->pChild, pzErr);
  }
  else
    if (memcmp(pAux->pMod->xConnect_signature, xConnect_signatures[xConnect_binfoConnect_enum], sizeof(pAux->pMod->xConnect_signature)) == 0) {
      rc = binfoConnect(db, pAux->pChildAux, argc, argv, &pNew->pChild, pzErr);
    }
  else
    if (memcmp(pAux->pMod->xConnect_signature, xConnect_signatures[xConnect_bytecodevtabConnect_enum], sizeof(pAux->pMod->xConnect_signature)) == 0) {
      rc = bytecodevtabConnect(db, pAux->pChildAux, argc, argv, &pNew->pChild,
                               pzErr);
    }
  else
    if (memcmp(pAux->pMod->xConnect_signature, xConnect_signatures[xConnect_carrayConnect_enum], sizeof(pAux->pMod->xConnect_signature)) == 0) {
      rc = carrayConnect(db, pAux->pChildAux, argc, argv, &pNew->pChild,
                         pzErr);
    }
  else
    if (memcmp(pAux->pMod->xConnect_signature, xConnect_signatures[xConnect_cidxConnect_enum], sizeof(pAux->pMod->xConnect_signature)) == 0) {
      rc = cidxConnect(db, pAux->pChildAux, argc, argv, &pNew->pChild, pzErr);
    }
  else
    if (memcmp(pAux->pMod->xConnect_signature, xConnect_signatures[xConnect_closureConnect_enum], sizeof(pAux->pMod->xConnect_signature)) == 0) {
      rc = closureConnect(db, pAux->pChildAux, argc, argv, &pNew->pChild,
                          pzErr);
    }
  else
    if (memcmp(pAux->pMod->xConnect_signature, xConnect_signatures[xConnect_completionConnect_enum], sizeof(pAux->pMod->xConnect_signature)) == 0) {
      rc = completionConnect(db, pAux->pChildAux, argc, argv, &pNew->pChild,
                             pzErr);
    }
  else
    if (memcmp(pAux->pMod->xConnect_signature, xConnect_signatures[xConnect_csvtabConnect_enum], sizeof(pAux->pMod->xConnect_signature)) == 0) {
      rc = csvtabConnect(db, pAux->pChildAux, argc, argv, &pNew->pChild,
                         pzErr);
    }
  else
    if (memcmp(pAux->pMod->xConnect_signature, xConnect_signatures[xConnect_dbdataConnect_enum], sizeof(pAux->pMod->xConnect_signature)) == 0) {
      rc = dbdataConnect(db, pAux->pChildAux, argc, argv, &pNew->pChild,
                         pzErr);
    }
  else
    if (memcmp(pAux->pMod->xConnect_signature, xConnect_signatures[xConnect_dbpageConnect_enum], sizeof(pAux->pMod->xConnect_signature)) == 0) {
      rc = dbpageConnect(db, pAux->pChildAux, argc, argv, &pNew->pChild,
                         pzErr);
    }
  else
    if (memcmp(pAux->pMod->xConnect_signature, xConnect_signatures[xConnect_deltaparsevtabConnect_enum], sizeof(pAux->pMod->xConnect_signature)) == 0) {
      rc = deltaparsevtabConnect(db, pAux->pChildAux, argc, argv,
                                 &pNew->pChild, pzErr);
    }
  else
    if (memcmp(pAux->pMod->xConnect_signature, xConnect_signatures[xConnect_echoConnect_enum], sizeof(pAux->pMod->xConnect_signature)) == 0) {
      rc = echoConnect(db, pAux->pChildAux, argc, argv, &pNew->pChild, pzErr);
    }
  else
    if (memcmp(pAux->pMod->xConnect_signature, xConnect_signatures[xConnect_expertConnect_enum], sizeof(pAux->pMod->xConnect_signature)) == 0) {
      rc = expertConnect(db, pAux->pChildAux, argc, argv, &pNew->pChild,
                         pzErr);
    }
  else
    if (memcmp(pAux->pMod->xConnect_signature, xConnect_signatures[xConnect_explainConnect_enum], sizeof(pAux->pMod->xConnect_signature)) == 0) {
      rc = explainConnect(db, pAux->pChildAux, argc, argv, &pNew->pChild,
                          pzErr);
    }
  else
    if (memcmp(pAux->pMod->xConnect_signature, xConnect_signatures[xConnect_fsConnect_enum], sizeof(pAux->pMod->xConnect_signature)) == 0) {
      rc = fsConnect(db, pAux->pChildAux, argc, argv, &pNew->pChild, pzErr);
    }
  else
    if (memcmp(pAux->pMod->xConnect_signature, xConnect_signatures[xConnect_fsdirConnect_enum], sizeof(pAux->pMod->xConnect_signature)) == 0) {
      rc = fsdirConnect(db, pAux->pChildAux, argc, argv, &pNew->pChild, pzErr);
    }
  else
    if (memcmp(pAux->pMod->xConnect_signature, xConnect_signatures[xConnect_fstreeConnect_enum], sizeof(pAux->pMod->xConnect_signature)) == 0) {
      rc = fstreeConnect(db, pAux->pChildAux, argc, argv, &pNew->pChild,
                         pzErr);
    }
  else
    if (memcmp(pAux->pMod->xConnect_signature, xConnect_signatures[xConnect_fts3ConnectMethod_enum], sizeof(pAux->pMod->xConnect_signature)) == 0) {
      rc = fts3ConnectMethod(db, pAux->pChildAux, argc, argv, &pNew->pChild,
                             pzErr);
    }
  else
    if (memcmp(pAux->pMod->xConnect_signature, xConnect_signatures[xConnect_fts3auxConnectMethod_enum], sizeof(pAux->pMod->xConnect_signature)) == 0) {
      rc = fts3auxConnectMethod(db, pAux->pChildAux, argc, argv,
                                &pNew->pChild, pzErr);
    }
  else
    if (memcmp(pAux->pMod->xConnect_signature, xConnect_signatures[xConnect_fts3termConnectMethod_enum], sizeof(pAux->pMod->xConnect_signature)) == 0) {
      rc = fts3termConnectMethod(db, pAux->pChildAux, argc, argv,
                                 &pNew->pChild, pzErr);
    }
  else
    if (memcmp(pAux->pMod->xConnect_signature, xConnect_signatures[xConnect_fts3tokConnectMethod_enum], sizeof(pAux->pMod->xConnect_signature)) == 0) {
      rc = fts3tokConnectMethod(db, pAux->pChildAux, argc, argv,
                                &pNew->pChild, pzErr);
    }
  else
    if (memcmp(pAux->pMod->xConnect_signature, xConnect_signatures[xConnect_fuzzerConnect_enum], sizeof(pAux->pMod->xConnect_signature)) == 0) {
      rc = fuzzerConnect(db, pAux->pChildAux, argc, argv, &pNew->pChild,
                         pzErr);
    }
  else
    if (memcmp(pAux->pMod->xConnect_signature, xConnect_signatures[xConnect_geopolyConnect_enum], sizeof(pAux->pMod->xConnect_signature)) == 0) {
      rc = geopolyConnect(db, pAux->pChildAux, argc, argv, &pNew->pChild,
                          pzErr);
    }
  else
    if (memcmp(pAux->pMod->xConnect_signature, xConnect_signatures[xConnect_intarrayCreate_enum], sizeof(pAux->pMod->xConnect_signature)) == 0) {
      rc = intarrayCreate(db, pAux->pChildAux, argc, argv, &pNew->pChild,
                          pzErr);
    }
  else
    if (memcmp(pAux->pMod->xConnect_signature, xConnect_signatures[xConnect_jsonEachConnect_enum], sizeof(pAux->pMod->xConnect_signature)) == 0) {
      rc = jsonEachConnect(db, pAux->pChildAux, argc, argv, &pNew->pChild,
                           pzErr);
    }
  else
    if (memcmp(pAux->pMod->xConnect_signature, xConnect_signatures[xConnect_memstatConnect_enum], sizeof(pAux->pMod->xConnect_signature)) == 0) {
      rc = memstatConnect(db, pAux->pChildAux, argc, argv, &pNew->pChild,
                          pzErr);
    }
  else
    if (memcmp(pAux->pMod->xConnect_signature, xConnect_signatures[xConnect_pragmaVtabConnect_enum], sizeof(pAux->pMod->xConnect_signature)) == 0) {
      rc = pragmaVtabConnect(db, pAux->pChildAux, argc, argv, &pNew->pChild,
                             pzErr);
    }
  else
    if (memcmp(pAux->pMod->xConnect_signature, xConnect_signatures[xConnect_prefixesConnect_enum], sizeof(pAux->pMod->xConnect_signature)) == 0) {
      rc = prefixesConnect(db, pAux->pChildAux, argc, argv, &pNew->pChild,
                           pzErr);
    }
  else
    if (memcmp(pAux->pMod->xConnect_signature, xConnect_signatures[xConnect_qpvtabConnect_enum], sizeof(pAux->pMod->xConnect_signature)) == 0) {
      rc = qpvtabConnect(db, pAux->pChildAux, argc, argv, &pNew->pChild,
                         pzErr);
    }
  else
    if (memcmp(pAux->pMod->xConnect_signature, xConnect_signatures[xConnect_rtreeConnect_enum], sizeof(pAux->pMod->xConnect_signature)) == 0) {
      rc = rtreeConnect(db, pAux->pChildAux, argc, argv, &pNew->pChild, pzErr);
    }
  else
    if (memcmp(pAux->pMod->xConnect_signature, xConnect_signatures[xConnect_schemaCreate_enum], sizeof(pAux->pMod->xConnect_signature)) == 0) {
      rc = schemaCreate(db, pAux->pChildAux, argc, argv, &pNew->pChild, pzErr);
    }
  else
    if (memcmp(pAux->pMod->xConnect_signature, xConnect_signatures[xConnect_seriesConnect_enum], sizeof(pAux->pMod->xConnect_signature)) == 0) {
      rc = seriesConnect(db, pAux->pChildAux, argc, argv, &pNew->pChild,
                         pzErr);
    }
  else
    if (memcmp(pAux->pMod->xConnect_signature, xConnect_signatures[xConnect_spellfix1Connect_enum], sizeof(pAux->pMod->xConnect_signature)) == 0) {
      rc = spellfix1Connect(db, pAux->pChildAux, argc, argv, &pNew->pChild,
                            pzErr);
    }
  else
    if (memcmp(pAux->pMod->xConnect_signature, xConnect_signatures[xConnect_statConnect_enum], sizeof(pAux->pMod->xConnect_signature)) == 0) {
      rc = statConnect(db, pAux->pChildAux, argc, argv, &pNew->pChild, pzErr);
    }
  else
    if (memcmp(pAux->pMod->xConnect_signature, xConnect_signatures[xConnect_stmtConnect_enum], sizeof(pAux->pMod->xConnect_signature)) == 0) {
      rc = stmtConnect(db, pAux->pChildAux, argc, argv, &pNew->pChild, pzErr);
    }
  else
    if (memcmp(pAux->pMod->xConnect_signature, xConnect_signatures[xConnect_tclConnect_enum], sizeof(pAux->pMod->xConnect_signature)) == 0) {
      rc = tclConnect(db, pAux->pChildAux, argc, argv, &pNew->pChild, pzErr);
    }
  else
    if (memcmp(pAux->pMod->xConnect_signature, xConnect_signatures[xConnect_tclvarConnect_enum], sizeof(pAux->pMod->xConnect_signature)) == 0) {
      rc = tclvarConnect(db, pAux->pChildAux, argc, argv, &pNew->pChild,
                         pzErr);
    }
  else
    if (memcmp(pAux->pMod->xConnect_signature, xConnect_signatures[xConnect_templatevtabConnect_enum], sizeof(pAux->pMod->xConnect_signature)) == 0) {
      rc = templatevtabConnect(db, pAux->pChildAux, argc, argv, &pNew->pChild,
                               pzErr);
    }
  else
    if (memcmp(pAux->pMod->xConnect_signature, xConnect_signatures[xConnect_unionConnect_enum], sizeof(pAux->pMod->xConnect_signature)) == 0) {
      rc = unionConnect(db, pAux->pChildAux, argc, argv, &pNew->pChild, pzErr);
    }
  else
    if (memcmp(pAux->pMod->xConnect_signature, xConnect_signatures[xConnect_vlogConnect_enum], sizeof(pAux->pMod->xConnect_signature)) == 0) {
      rc = vlogConnect(db, pAux->pChildAux, argc, argv, &pNew->pChild, pzErr);
    }
  else
    if (memcmp(pAux->pMod->xConnect_signature, xConnect_signatures[xConnect_vstattabConnect_enum], sizeof(pAux->pMod->xConnect_signature)) == 0) {
      rc = vstattabConnect(db, pAux->pChildAux, argc, argv, &pNew->pChild,
                           pzErr);
    }
  else
    if (memcmp(pAux->pMod->xConnect_signature, xConnect_signatures[xConnect_vtablogConnect_enum], sizeof(pAux->pMod->xConnect_signature)) == 0) {
      rc = vtablogConnect(db, pAux->pChildAux, argc, argv, &pNew->pChild,
                          pzErr);
    }
  else
    if (memcmp(pAux->pMod->xConnect_signature, xConnect_signatures[xConnect_wholenumberConnect_enum], sizeof(pAux->pMod->xConnect_signature)) == 0) {
      rc = wholenumberConnect(db, pAux->pChildAux, argc, argv, &pNew->pChild,
                              pzErr);
    }
  else
    if (memcmp(pAux->pMod->xConnect_signature, xConnect_signatures[xConnect_zipfileConnect_enum], sizeof(pAux->pMod->xConnect_signature)) == 0) {
      rc = zipfileConnect(db, pAux->pChildAux, argc, argv, &pNew->pChild,
                          pzErr);
    }
  if( rc ){
    sqlite3_free(pNew);
    *ppVtab = 0;
    return rc;
  }
  pNew->pAux = pAux;
  pNew->ppPrev = &pAux->pAllVtab;
  pNew->pNext = pAux->pAllVtab;
  if( pAux->pAllVtab ) pAux->pAllVtab->ppPrev = &pNew->pNext;
  pAux->pAllVtab = pNew;
  return rc;
}

static int vtshimBestIndex(
  sqlite3_vtab *pBase,
  sqlite3_index_info *pIdxInfo
){
  vtshim_vtab *pVtab = (vtshim_vtab*)pBase;
  vtshim_aux *pAux = pVtab->pAux;
  int rc;
  if( pAux->bDisposed ) return SQLITE_ERROR;
  if (memcmp(pAux->pMod->xBestIndex_signature, xBestIndex_signatures[xBestIndex_bytecodevtabBestIndex_enum], sizeof(pAux->pMod->xBestIndex_signature)) == 0) {
    rc = bytecodevtabBestIndex(pVtab->pChild, pIdxInfo);
  }
  else
    if (memcmp(pAux->pMod->xBestIndex_signature, xBestIndex_signatures[xBestIndex_completionBestIndex_enum], sizeof(pAux->pMod->xBestIndex_signature)) == 0) {
      rc = completionBestIndex(pVtab->pChild, pIdxInfo);
    }
  else
    if (memcmp(pAux->pMod->xBestIndex_signature, xBestIndex_signatures[xBestIndex_dbdataBestIndex_enum], sizeof(pAux->pMod->xBestIndex_signature)) == 0) {
      rc = dbdataBestIndex(pVtab->pChild, pIdxInfo);
    }
  else
    if (memcmp(pAux->pMod->xBestIndex_signature, xBestIndex_signatures[xBestIndex_dbpageBestIndex_enum], sizeof(pAux->pMod->xBestIndex_signature)) == 0) {
      rc = dbpageBestIndex(pVtab->pChild, pIdxInfo);
    }
  else
    if (memcmp(pAux->pMod->xBestIndex_signature, xBestIndex_signatures[xBestIndex_expertBestIndex_enum], sizeof(pAux->pMod->xBestIndex_signature)) == 0) {
      rc = expertBestIndex(pVtab->pChild, pIdxInfo);
    }
  else
    if (memcmp(pAux->pMod->xBestIndex_signature, xBestIndex_signatures[xBestIndex_fsdirBestIndex_enum], sizeof(pAux->pMod->xBestIndex_signature)) == 0) {
      rc = fsdirBestIndex(pVtab->pChild, pIdxInfo);
    }
  else
    if (memcmp(pAux->pMod->xBestIndex_signature, xBestIndex_signatures[xBestIndex_fts3BestIndexMethod_enum], sizeof(pAux->pMod->xBestIndex_signature)) == 0) {
      rc = fts3BestIndexMethod(pVtab->pChild, pIdxInfo);
    }
  else
    if (memcmp(pAux->pMod->xBestIndex_signature, xBestIndex_signatures[xBestIndex_fts3auxBestIndexMethod_enum], sizeof(pAux->pMod->xBestIndex_signature)) == 0) {
      rc = fts3auxBestIndexMethod(pVtab->pChild, pIdxInfo);
    }
  else
    if (memcmp(pAux->pMod->xBestIndex_signature, xBestIndex_signatures[xBestIndex_fts3tokBestIndexMethod_enum], sizeof(pAux->pMod->xBestIndex_signature)) == 0) {
      rc = fts3tokBestIndexMethod(pVtab->pChild, pIdxInfo);
    }
  else
    if (memcmp(pAux->pMod->xBestIndex_signature, xBestIndex_signatures[xBestIndex_jsonEachBestIndex_enum], sizeof(pAux->pMod->xBestIndex_signature)) == 0) {
      rc = jsonEachBestIndex(pVtab->pChild, pIdxInfo);
    }
  else
    if (memcmp(pAux->pMod->xBestIndex_signature, xBestIndex_signatures[xBestIndex_pragmaVtabBestIndex_enum], sizeof(pAux->pMod->xBestIndex_signature)) == 0) {
      rc = pragmaVtabBestIndex(pVtab->pChild, pIdxInfo);
    }
  else
    if (memcmp(pAux->pMod->xBestIndex_signature, xBestIndex_signatures[xBestIndex_rtreeBestIndex_enum], sizeof(pAux->pMod->xBestIndex_signature)) == 0) {
      rc = rtreeBestIndex(pVtab->pChild, pIdxInfo);
    }
  else
    if (memcmp(pAux->pMod->xBestIndex_signature, xBestIndex_signatures[xBestIndex_seriesBestIndex_enum], sizeof(pAux->pMod->xBestIndex_signature)) == 0) {
      rc = seriesBestIndex(pVtab->pChild, pIdxInfo);
    }
  else
    if (memcmp(pAux->pMod->xBestIndex_signature, xBestIndex_signatures[xBestIndex_statBestIndex_enum], sizeof(pAux->pMod->xBestIndex_signature)) == 0) {
      rc = statBestIndex(pVtab->pChild, pIdxInfo);
    }
  else
    if (memcmp(pAux->pMod->xBestIndex_signature, xBestIndex_signatures[xBestIndex_stmtBestIndex_enum], sizeof(pAux->pMod->xBestIndex_signature)) == 0) {
      rc = stmtBestIndex(pVtab->pChild, pIdxInfo);
    }
  else
    if (memcmp(pAux->pMod->xBestIndex_signature, xBestIndex_signatures[xBestIndex_zipfileBestIndex_enum], sizeof(pAux->pMod->xBestIndex_signature)) == 0) {
      rc = zipfileBestIndex(pVtab->pChild, pIdxInfo);
    }
  if( rc!=SQLITE_OK ){
    VTSHIM_COPY_ERRMSG();
  }
  return rc;
}

static int vtshimDisconnect(sqlite3_vtab *pBase){
  vtshim_vtab *pVtab = (vtshim_vtab*)pBase;
  vtshim_aux *pAux = pVtab->pAux;
  int rc = SQLITE_OK;
  if( !pAux->bDisposed ){
    if (memcmp(pAux->pMod->xDisconnect_signature, xDisconnect_signatures[xDisconnect_bytecodevtabDisconnect_enum], sizeof(pAux->pMod->xDisconnect_signature)) == 0) {
      rc = bytecodevtabDisconnect(pVtab->pChild);
    }
    else
      if (memcmp(pAux->pMod->xDisconnect_signature, xDisconnect_signatures[xDisconnect_completionDisconnect_enum], sizeof(pAux->pMod->xDisconnect_signature)) == 0) {
        rc = completionDisconnect(pVtab->pChild);
      }
    else
      if (memcmp(pAux->pMod->xDisconnect_signature, xDisconnect_signatures[xDisconnect_dbdataDisconnect_enum], sizeof(pAux->pMod->xDisconnect_signature)) == 0) {
        rc = dbdataDisconnect(pVtab->pChild);
      }
    else
      if (memcmp(pAux->pMod->xDisconnect_signature, xDisconnect_signatures[xDisconnect_dbpageDisconnect_enum], sizeof(pAux->pMod->xDisconnect_signature)) == 0) {
        rc = dbpageDisconnect(pVtab->pChild);
      }
    else
      if (memcmp(pAux->pMod->xDisconnect_signature, xDisconnect_signatures[xDisconnect_expertDisconnect_enum], sizeof(pAux->pMod->xDisconnect_signature)) == 0) {
        rc = expertDisconnect(pVtab->pChild);
      }
    else
      if (memcmp(pAux->pMod->xDisconnect_signature, xDisconnect_signatures[xDisconnect_fsdirDisconnect_enum], sizeof(pAux->pMod->xDisconnect_signature)) == 0) {
        rc = fsdirDisconnect(pVtab->pChild);
      }
    else
      if (memcmp(pAux->pMod->xDisconnect_signature, xDisconnect_signatures[xDisconnect_fts3DisconnectMethod_enum], sizeof(pAux->pMod->xDisconnect_signature)) == 0) {
        rc = fts3DisconnectMethod(pVtab->pChild);
      }
    else
      if (memcmp(pAux->pMod->xDisconnect_signature, xDisconnect_signatures[xDisconnect_fts3auxDisconnectMethod_enum], sizeof(pAux->pMod->xDisconnect_signature)) == 0) {
        rc = fts3auxDisconnectMethod(pVtab->pChild);
      }
    else
      if (memcmp(pAux->pMod->xDisconnect_signature, xDisconnect_signatures[xDisconnect_fts3tokDisconnectMethod_enum], sizeof(pAux->pMod->xDisconnect_signature)) == 0) {
        rc = fts3tokDisconnectMethod(pVtab->pChild);
      }
    else
      if (memcmp(pAux->pMod->xDisconnect_signature, xDisconnect_signatures[xDisconnect_jsonEachDisconnect_enum], sizeof(pAux->pMod->xDisconnect_signature)) == 0) {
        rc = jsonEachDisconnect(pVtab->pChild);
      }
    else
      if (memcmp(pAux->pMod->xDisconnect_signature, xDisconnect_signatures[xDisconnect_pragmaVtabDisconnect_enum], sizeof(pAux->pMod->xDisconnect_signature)) == 0) {
        rc = pragmaVtabDisconnect(pVtab->pChild);
      }
    else
      if (memcmp(pAux->pMod->xDisconnect_signature, xDisconnect_signatures[xDisconnect_rtreeDisconnect_enum], sizeof(pAux->pMod->xDisconnect_signature)) == 0) {
        rc = rtreeDisconnect(pVtab->pChild);
      }
    else
      if (memcmp(pAux->pMod->xDisconnect_signature, xDisconnect_signatures[xDisconnect_seriesDisconnect_enum], sizeof(pAux->pMod->xDisconnect_signature)) == 0) {
        rc = seriesDisconnect(pVtab->pChild);
      }
    else
      if (memcmp(pAux->pMod->xDisconnect_signature, xDisconnect_signatures[xDisconnect_statDisconnect_enum], sizeof(pAux->pMod->xDisconnect_signature)) == 0) {
        rc = statDisconnect(pVtab->pChild);
      }
    else
      if (memcmp(pAux->pMod->xDisconnect_signature, xDisconnect_signatures[xDisconnect_stmtDisconnect_enum], sizeof(pAux->pMod->xDisconnect_signature)) == 0) {
        rc = stmtDisconnect(pVtab->pChild);
      }
    else
      if (memcmp(pAux->pMod->xDisconnect_signature, xDisconnect_signatures[xDisconnect_zipfileDisconnect_enum], sizeof(pAux->pMod->xDisconnect_signature)) == 0) {
        rc = zipfileDisconnect(pVtab->pChild);
      }
  }
  if( pVtab->pNext ) pVtab->pNext->ppPrev = pVtab->ppPrev;
  *pVtab->ppPrev = pVtab->pNext;
  sqlite3_free(pVtab);
  return rc;
}

static int vtshimDestroy(sqlite3_vtab *pBase){
  vtshim_vtab *pVtab = (vtshim_vtab*)pBase;
  vtshim_aux *pAux = pVtab->pAux;
  int rc = SQLITE_OK;
  if( !pAux->bDisposed ){
    if (memcmp(pAux->pMod->xDestroy_signature, xDestroy_signatures[xDestroy_0_enum], sizeof(pAux->pMod->xDestroy_signature)) == 0) {
      rc = 0;
    }
    else
      if (memcmp(pAux->pMod->xDestroy_signature, xDestroy_signatures[xDestroy_dbpageDisconnect_enum], sizeof(pAux->pMod->xDestroy_signature)) == 0) {
        rc = dbpageDisconnect(pVtab->pChild);
      }
    else
      if (memcmp(pAux->pMod->xDestroy_signature, xDestroy_signatures[xDestroy_expertDisconnect_enum], sizeof(pAux->pMod->xDestroy_signature)) == 0) {
        rc = expertDisconnect(pVtab->pChild);
      }
    else
      if (memcmp(pAux->pMod->xDestroy_signature, xDestroy_signatures[xDestroy_fsdirDisconnect_enum], sizeof(pAux->pMod->xDestroy_signature)) == 0) {
        rc = fsdirDisconnect(pVtab->pChild);
      }
    else
      if (memcmp(pAux->pMod->xDestroy_signature, xDestroy_signatures[xDestroy_fts3DestroyMethod_enum], sizeof(pAux->pMod->xDestroy_signature)) == 0) {
        rc = fts3DestroyMethod(pVtab->pChild);
      }
    else
      if (memcmp(pAux->pMod->xDestroy_signature, xDestroy_signatures[xDestroy_fts3auxDisconnectMethod_enum], sizeof(pAux->pMod->xDestroy_signature)) == 0) {
        rc = fts3auxDisconnectMethod(pVtab->pChild);
      }
    else
      if (memcmp(pAux->pMod->xDestroy_signature, xDestroy_signatures[xDestroy_fts3tokDisconnectMethod_enum], sizeof(pAux->pMod->xDestroy_signature)) == 0) {
        rc = fts3tokDisconnectMethod(pVtab->pChild);
      }
    else
      if (memcmp(pAux->pMod->xDestroy_signature, xDestroy_signatures[xDestroy_pcache1Destroy_enum], sizeof(pAux->pMod->xDestroy_signature)) == 0) {
        rc = pcache1Destroy(pVtab->pChild);
      }
    else
      if (memcmp(pAux->pMod->xDestroy_signature, xDestroy_signatures[xDestroy_pcachetraceDestroy_enum], sizeof(pAux->pMod->xDestroy_signature)) == 0) {
        rc = pcachetraceDestroy(pVtab->pChild);
      }
    else
      if (memcmp(pAux->pMod->xDestroy_signature, xDestroy_signatures[xDestroy_porterDestroy_enum], sizeof(pAux->pMod->xDestroy_signature)) == 0) {
        rc = porterDestroy(pVtab->pChild);
      }
    else
      if (memcmp(pAux->pMod->xDestroy_signature, xDestroy_signatures[xDestroy_rtreeDestroy_enum], sizeof(pAux->pMod->xDestroy_signature)) == 0) {
        rc = rtreeDestroy(pVtab->pChild);
      }
    else
      if (memcmp(pAux->pMod->xDestroy_signature, xDestroy_signatures[xDestroy_simpleDestroy_enum], sizeof(pAux->pMod->xDestroy_signature)) == 0) {
        rc = simpleDestroy(pVtab->pChild);
      }
    else
      if (memcmp(pAux->pMod->xDestroy_signature, xDestroy_signatures[xDestroy_statDisconnect_enum], sizeof(pAux->pMod->xDestroy_signature)) == 0) {
        rc = statDisconnect(pVtab->pChild);
      }
    else
      if (memcmp(pAux->pMod->xDestroy_signature, xDestroy_signatures[xDestroy_unicodeDestroy_enum], sizeof(pAux->pMod->xDestroy_signature)) == 0) {
        rc = unicodeDestroy(pVtab->pChild);
      }
    else
      if (memcmp(pAux->pMod->xDestroy_signature, xDestroy_signatures[xDestroy_zipfileDisconnect_enum], sizeof(pAux->pMod->xDestroy_signature)) == 0) {
        rc = zipfileDisconnect(pVtab->pChild);
      }
  }
  if( pVtab->pNext ) pVtab->pNext->ppPrev = pVtab->ppPrev;
  *pVtab->ppPrev = pVtab->pNext;
  sqlite3_free(pVtab);
  return rc;
}

static int vtshimOpen(sqlite3_vtab *pBase, sqlite3_vtab_cursor **ppCursor){
  vtshim_vtab *pVtab = (vtshim_vtab*)pBase;
  vtshim_aux *pAux = pVtab->pAux;
  vtshim_cursor *pCur;
  int rc;
  *ppCursor = 0;
  if( pAux->bDisposed ) return SQLITE_ERROR;
  pCur = sqlite3_malloc( sizeof(*pCur) );
  if( pCur==0 ) return SQLITE_NOMEM;
  memset(pCur, 0, sizeof(*pCur));
  if (memcmp(pAux->pMod->xOpen_signature, xOpen_signatures[xOpen_amatchOpen_enum], sizeof(pAux->pMod->xOpen_signature)) == 0) {
    rc = amatchOpen(pVtab->pChild, &pCur->pChild);
  }
  else
    if (memcmp(pAux->pMod->xOpen_signature, xOpen_signatures[xOpen_apndOpen_enum], sizeof(pAux->pMod->xOpen_signature)) == 0) {
      rc = apndOpen(pVtab->pChild, &pCur->pChild);
    }
  else
    if (memcmp(pAux->pMod->xOpen_signature, xOpen_signatures[xOpen_binfoOpen_enum], sizeof(pAux->pMod->xOpen_signature)) == 0) {
      rc = binfoOpen(pVtab->pChild, &pCur->pChild);
    }
  else
    if (memcmp(pAux->pMod->xOpen_signature, xOpen_signatures[xOpen_bytecodevtabOpen_enum], sizeof(pAux->pMod->xOpen_signature)) == 0) {
      rc = bytecodevtabOpen(pVtab->pChild, &pCur->pChild);
    }
  else
    if (memcmp(pAux->pMod->xOpen_signature, xOpen_signatures[xOpen_carrayOpen_enum], sizeof(pAux->pMod->xOpen_signature)) == 0) {
      rc = carrayOpen(pVtab->pChild, &pCur->pChild);
    }
  else
    if (memcmp(pAux->pMod->xOpen_signature, xOpen_signatures[xOpen_cidxOpen_enum], sizeof(pAux->pMod->xOpen_signature)) == 0) {
      rc = cidxOpen(pVtab->pChild, &pCur->pChild);
    }
  else
    if (memcmp(pAux->pMod->xOpen_signature, xOpen_signatures[xOpen_closureOpen_enum], sizeof(pAux->pMod->xOpen_signature)) == 0) {
      rc = closureOpen(pVtab->pChild, &pCur->pChild);
    }
  else
    if (memcmp(pAux->pMod->xOpen_signature, xOpen_signatures[xOpen_completionOpen_enum], sizeof(pAux->pMod->xOpen_signature)) == 0) {
      rc = completionOpen(pVtab->pChild, &pCur->pChild);
    }
  else
    if (memcmp(pAux->pMod->xOpen_signature, xOpen_signatures[xOpen_csvtabOpen_enum], sizeof(pAux->pMod->xOpen_signature)) == 0) {
      rc = csvtabOpen(pVtab->pChild, &pCur->pChild);
    }
  else
    if (memcmp(pAux->pMod->xOpen_signature, xOpen_signatures[xOpen_dbdataOpen_enum], sizeof(pAux->pMod->xOpen_signature)) == 0) {
      rc = dbdataOpen(pVtab->pChild, &pCur->pChild);
    }
  else
    if (memcmp(pAux->pMod->xOpen_signature, xOpen_signatures[xOpen_dbpageOpen_enum], sizeof(pAux->pMod->xOpen_signature)) == 0) {
      rc = dbpageOpen(pVtab->pChild, &pCur->pChild);
    }
  else
    if (memcmp(pAux->pMod->xOpen_signature, xOpen_signatures[xOpen_deltaparsevtabOpen_enum], sizeof(pAux->pMod->xOpen_signature)) == 0) {
      rc = deltaparsevtabOpen(pVtab->pChild, &pCur->pChild);
    }
  else
    if (memcmp(pAux->pMod->xOpen_signature, xOpen_signatures[xOpen_echoOpen_enum], sizeof(pAux->pMod->xOpen_signature)) == 0) {
      rc = echoOpen(pVtab->pChild, &pCur->pChild);
    }
  else
    if (memcmp(pAux->pMod->xOpen_signature, xOpen_signatures[xOpen_expertOpen_enum], sizeof(pAux->pMod->xOpen_signature)) == 0) {
      rc = expertOpen(pVtab->pChild, &pCur->pChild);
    }
  else
    if (memcmp(pAux->pMod->xOpen_signature, xOpen_signatures[xOpen_explainOpen_enum], sizeof(pAux->pMod->xOpen_signature)) == 0) {
      rc = explainOpen(pVtab->pChild, &pCur->pChild);
    }
  else
    if (memcmp(pAux->pMod->xOpen_signature, xOpen_signatures[xOpen_fsOpen_enum], sizeof(pAux->pMod->xOpen_signature)) == 0) {
      rc = fsOpen(pVtab->pChild, &pCur->pChild);
    }
  else
    if (memcmp(pAux->pMod->xOpen_signature, xOpen_signatures[xOpen_fsdirOpen_enum], sizeof(pAux->pMod->xOpen_signature)) == 0) {
      rc = fsdirOpen(pVtab->pChild, &pCur->pChild);
    }
  else
    if (memcmp(pAux->pMod->xOpen_signature, xOpen_signatures[xOpen_fstreeOpen_enum], sizeof(pAux->pMod->xOpen_signature)) == 0) {
      rc = fstreeOpen(pVtab->pChild, &pCur->pChild);
    }
  else
    if (memcmp(pAux->pMod->xOpen_signature, xOpen_signatures[xOpen_fts3OpenMethod_enum], sizeof(pAux->pMod->xOpen_signature)) == 0) {
      rc = fts3OpenMethod(pVtab->pChild, &pCur->pChild);
    }
  else
    if (memcmp(pAux->pMod->xOpen_signature, xOpen_signatures[xOpen_fts3auxOpenMethod_enum], sizeof(pAux->pMod->xOpen_signature)) == 0) {
      rc = fts3auxOpenMethod(pVtab->pChild, &pCur->pChild);
    }
  else
    if (memcmp(pAux->pMod->xOpen_signature, xOpen_signatures[xOpen_fts3termOpenMethod_enum], sizeof(pAux->pMod->xOpen_signature)) == 0) {
      rc = fts3termOpenMethod(pVtab->pChild, &pCur->pChild);
    }
  else
    if (memcmp(pAux->pMod->xOpen_signature, xOpen_signatures[xOpen_fts3tokOpenMethod_enum], sizeof(pAux->pMod->xOpen_signature)) == 0) {
      rc = fts3tokOpenMethod(pVtab->pChild, &pCur->pChild);
    }
  else
    if (memcmp(pAux->pMod->xOpen_signature, xOpen_signatures[xOpen_fuzzerOpen_enum], sizeof(pAux->pMod->xOpen_signature)) == 0) {
      rc = fuzzerOpen(pVtab->pChild, &pCur->pChild);
    }
  else
    if (memcmp(pAux->pMod->xOpen_signature, xOpen_signatures[xOpen_intarrayOpen_enum], sizeof(pAux->pMod->xOpen_signature)) == 0) {
      rc = intarrayOpen(pVtab->pChild, &pCur->pChild);
    }
  else
    if (memcmp(pAux->pMod->xOpen_signature, xOpen_signatures[xOpen_jsonEachOpen_enum], sizeof(pAux->pMod->xOpen_signature)) == 0) {
      rc = jsonEachOpen(pVtab->pChild, &pCur->pChild);
    }
  else
    if (memcmp(pAux->pMod->xOpen_signature, xOpen_signatures[xOpen_jsonEachOpenEach_enum], sizeof(pAux->pMod->xOpen_signature)) == 0) {
      rc = jsonEachOpenEach(pVtab->pChild, &pCur->pChild);
    }
  else
    if (memcmp(pAux->pMod->xOpen_signature, xOpen_signatures[xOpen_jsonEachOpenTree_enum], sizeof(pAux->pMod->xOpen_signature)) == 0) {
      rc = jsonEachOpenTree(pVtab->pChild, &pCur->pChild);
    }
  else
    if (memcmp(pAux->pMod->xOpen_signature, xOpen_signatures[xOpen_memdbOpen_enum], sizeof(pAux->pMod->xOpen_signature)) == 0) {
      rc = memdbOpen(pVtab->pChild, &pCur->pChild);
    }
  else
    if (memcmp(pAux->pMod->xOpen_signature, xOpen_signatures[xOpen_memstatOpen_enum], sizeof(pAux->pMod->xOpen_signature)) == 0) {
      rc = memstatOpen(pVtab->pChild, &pCur->pChild);
    }
  else
    if (memcmp(pAux->pMod->xOpen_signature, xOpen_signatures[xOpen_porterOpen_enum], sizeof(pAux->pMod->xOpen_signature)) == 0) {
      rc = porterOpen(pVtab->pChild, &pCur->pChild);
    }
  else
    if (memcmp(pAux->pMod->xOpen_signature, xOpen_signatures[xOpen_pragmaVtabOpen_enum], sizeof(pAux->pMod->xOpen_signature)) == 0) {
      rc = pragmaVtabOpen(pVtab->pChild, &pCur->pChild);
    }
  else
    if (memcmp(pAux->pMod->xOpen_signature, xOpen_signatures[xOpen_prefixesOpen_enum], sizeof(pAux->pMod->xOpen_signature)) == 0) {
      rc = prefixesOpen(pVtab->pChild, &pCur->pChild);
    }
  else
    if (memcmp(pAux->pMod->xOpen_signature, xOpen_signatures[xOpen_qpvtabOpen_enum], sizeof(pAux->pMod->xOpen_signature)) == 0) {
      rc = qpvtabOpen(pVtab->pChild, &pCur->pChild);
    }
  else
    if (memcmp(pAux->pMod->xOpen_signature, xOpen_signatures[xOpen_rtreeOpen_enum], sizeof(pAux->pMod->xOpen_signature)) == 0) {
      rc = rtreeOpen(pVtab->pChild, &pCur->pChild);
    }
  else
    if (memcmp(pAux->pMod->xOpen_signature, xOpen_signatures[xOpen_schemaOpen_enum], sizeof(pAux->pMod->xOpen_signature)) == 0) {
      rc = schemaOpen(pVtab->pChild, &pCur->pChild);
    }
  else
    if (memcmp(pAux->pMod->xOpen_signature, xOpen_signatures[xOpen_seriesOpen_enum], sizeof(pAux->pMod->xOpen_signature)) == 0) {
      rc = seriesOpen(pVtab->pChild, &pCur->pChild);
    }
  else
    if (memcmp(pAux->pMod->xOpen_signature, xOpen_signatures[xOpen_simpleOpen_enum], sizeof(pAux->pMod->xOpen_signature)) == 0) {
      rc = simpleOpen(pVtab->pChild, &pCur->pChild);
    }
  else
    if (memcmp(pAux->pMod->xOpen_signature, xOpen_signatures[xOpen_spellfix1Open_enum], sizeof(pAux->pMod->xOpen_signature)) == 0) {
      rc = spellfix1Open(pVtab->pChild, &pCur->pChild);
    }
  else
    if (memcmp(pAux->pMod->xOpen_signature, xOpen_signatures[xOpen_statOpen_enum], sizeof(pAux->pMod->xOpen_signature)) == 0) {
      rc = statOpen(pVtab->pChild, &pCur->pChild);
    }
  else
    if (memcmp(pAux->pMod->xOpen_signature, xOpen_signatures[xOpen_stmtOpen_enum], sizeof(pAux->pMod->xOpen_signature)) == 0) {
      rc = stmtOpen(pVtab->pChild, &pCur->pChild);
    }
  else
    if (memcmp(pAux->pMod->xOpen_signature, xOpen_signatures[xOpen_tclOpen_enum], sizeof(pAux->pMod->xOpen_signature)) == 0) {
      rc = tclOpen(pVtab->pChild, &pCur->pChild);
    }
  else
    if (memcmp(pAux->pMod->xOpen_signature, xOpen_signatures[xOpen_tclvarOpen_enum], sizeof(pAux->pMod->xOpen_signature)) == 0) {
      rc = tclvarOpen(pVtab->pChild, &pCur->pChild);
    }
  else
    if (memcmp(pAux->pMod->xOpen_signature, xOpen_signatures[xOpen_templatevtabOpen_enum], sizeof(pAux->pMod->xOpen_signature)) == 0) {
      rc = templatevtabOpen(pVtab->pChild, &pCur->pChild);
    }
  else
    if (memcmp(pAux->pMod->xOpen_signature, xOpen_signatures[xOpen_unicodeOpen_enum], sizeof(pAux->pMod->xOpen_signature)) == 0) {
      rc = unicodeOpen(pVtab->pChild, &pCur->pChild);
    }
  else
    if (memcmp(pAux->pMod->xOpen_signature, xOpen_signatures[xOpen_unionOpen_enum], sizeof(pAux->pMod->xOpen_signature)) == 0) {
      rc = unionOpen(pVtab->pChild, &pCur->pChild);
    }
  else
    if (memcmp(pAux->pMod->xOpen_signature, xOpen_signatures[xOpen_vfstraceOpen_enum], sizeof(pAux->pMod->xOpen_signature)) == 0) {
      rc = vfstraceOpen(pVtab->pChild, &pCur->pChild);
    }
  else
    if (memcmp(pAux->pMod->xOpen_signature, xOpen_signatures[xOpen_vstattabOpen_enum], sizeof(pAux->pMod->xOpen_signature)) == 0) {
      rc = vstattabOpen(pVtab->pChild, &pCur->pChild);
    }
  else
    if (memcmp(pAux->pMod->xOpen_signature, xOpen_signatures[xOpen_vtablogOpen_enum], sizeof(pAux->pMod->xOpen_signature)) == 0) {
      rc = vtablogOpen(pVtab->pChild, &pCur->pChild);
    }
  else
    if (memcmp(pAux->pMod->xOpen_signature, xOpen_signatures[xOpen_wholenumberOpen_enum], sizeof(pAux->pMod->xOpen_signature)) == 0) {
      rc = wholenumberOpen(pVtab->pChild, &pCur->pChild);
    }
  else
    if (memcmp(pAux->pMod->xOpen_signature, xOpen_signatures[xOpen_zipfileOpen_enum], sizeof(pAux->pMod->xOpen_signature)) == 0) {
      rc = zipfileOpen(pVtab->pChild, &pCur->pChild);
    }
  else
    if (memcmp(pAux->pMod->xOpen_signature, xOpen_signatures[xOpen_unixOpen_enum], sizeof(pAux->pMod->xOpen_signature)) == 0) {
      rc = unixOpen(pVtab->pChild, &pCur->pChild);
    }
  if( rc ){
    sqlite3_free(pCur);
    VTSHIM_COPY_ERRMSG();
    return rc;
  }
  pCur->pChild->pVtab = pVtab->pChild;
  *ppCursor = &pCur->base;
  pCur->ppPrev = &pVtab->pAllCur;
  if( pVtab->pAllCur ) pVtab->pAllCur->ppPrev = &pCur->pNext;
  pCur->pNext = pVtab->pAllCur;
  pVtab->pAllCur = pCur;
  return SQLITE_OK;
}

static int vtshimClose(sqlite3_vtab_cursor *pX){
  vtshim_cursor *pCur = (vtshim_cursor*)pX;
  vtshim_vtab *pVtab = (vtshim_vtab*)pCur->base.pVtab;
  vtshim_aux *pAux = pVtab->pAux;
  int rc = SQLITE_OK;
  if( !pAux->bDisposed ){
    if (memcmp(pAux->pMod->xClose_signature, xClose_signatures[xClose_apndClose_enum], sizeof(pAux->pMod->xClose_signature)) == 0) {
      rc = apndClose(pCur->pChild);
    }
    else
      if (memcmp(pAux->pMod->xClose_signature, xClose_signatures[xClose_bytecodevtabClose_enum], sizeof(pAux->pMod->xClose_signature)) == 0) {
        rc = bytecodevtabClose(pCur->pChild);
      }
    else
      if (memcmp(pAux->pMod->xClose_signature, xClose_signatures[xClose_completionClose_enum], sizeof(pAux->pMod->xClose_signature)) == 0) {
        rc = completionClose(pCur->pChild);
      }
    else
      if (memcmp(pAux->pMod->xClose_signature, xClose_signatures[xClose_dbdataClose_enum], sizeof(pAux->pMod->xClose_signature)) == 0) {
        rc = dbdataClose(pCur->pChild);
      }
    else
      if (memcmp(pAux->pMod->xClose_signature, xClose_signatures[xClose_dbpageClose_enum], sizeof(pAux->pMod->xClose_signature)) == 0) {
        rc = dbpageClose(pCur->pChild);
      }
    else
      if (memcmp(pAux->pMod->xClose_signature, xClose_signatures[xClose_expertClose_enum], sizeof(pAux->pMod->xClose_signature)) == 0) {
        rc = expertClose(pCur->pChild);
      }
    else
      if (memcmp(pAux->pMod->xClose_signature, xClose_signatures[xClose_fsdirClose_enum], sizeof(pAux->pMod->xClose_signature)) == 0) {
        rc = fsdirClose(pCur->pChild);
      }
    else
      if (memcmp(pAux->pMod->xClose_signature, xClose_signatures[xClose_fts3CloseMethod_enum], sizeof(pAux->pMod->xClose_signature)) == 0) {
        rc = fts3CloseMethod(pCur->pChild);
      }
    else
      if (memcmp(pAux->pMod->xClose_signature, xClose_signatures[xClose_fts3auxCloseMethod_enum], sizeof(pAux->pMod->xClose_signature)) == 0) {
        rc = fts3auxCloseMethod(pCur->pChild);
      }
    else
      if (memcmp(pAux->pMod->xClose_signature, xClose_signatures[xClose_fts3tokCloseMethod_enum], sizeof(pAux->pMod->xClose_signature)) == 0) {
        rc = fts3tokCloseMethod(pCur->pChild);
      }
    else
      if (memcmp(pAux->pMod->xClose_signature, xClose_signatures[xClose_jsonEachClose_enum], sizeof(pAux->pMod->xClose_signature)) == 0) {
        rc = jsonEachClose(pCur->pChild);
      }
    else
      if (memcmp(pAux->pMod->xClose_signature, xClose_signatures[xClose_memdbClose_enum], sizeof(pAux->pMod->xClose_signature)) == 0) {
        rc = memdbClose(pCur->pChild);
      }
    else
      if (memcmp(pAux->pMod->xClose_signature, xClose_signatures[xClose_memjrnlClose_enum], sizeof(pAux->pMod->xClose_signature)) == 0) {
        rc = memjrnlClose(pCur->pChild);
      }
    else
      if (memcmp(pAux->pMod->xClose_signature, xClose_signatures[xClose_porterClose_enum], sizeof(pAux->pMod->xClose_signature)) == 0) {
        rc = porterClose(pCur->pChild);
      }
    else
      if (memcmp(pAux->pMod->xClose_signature, xClose_signatures[xClose_pragmaVtabClose_enum], sizeof(pAux->pMod->xClose_signature)) == 0) {
        rc = pragmaVtabClose(pCur->pChild);
      }
    else
      if (memcmp(pAux->pMod->xClose_signature, xClose_signatures[xClose_recoverVfsClose_enum], sizeof(pAux->pMod->xClose_signature)) == 0) {
        rc = recoverVfsClose(pCur->pChild);
      }
    else
      if (memcmp(pAux->pMod->xClose_signature, xClose_signatures[xClose_rtreeClose_enum], sizeof(pAux->pMod->xClose_signature)) == 0) {
        rc = rtreeClose(pCur->pChild);
      }
    else
      if (memcmp(pAux->pMod->xClose_signature, xClose_signatures[xClose_seriesClose_enum], sizeof(pAux->pMod->xClose_signature)) == 0) {
        rc = seriesClose(pCur->pChild);
      }
    else
      if (memcmp(pAux->pMod->xClose_signature, xClose_signatures[xClose_simpleClose_enum], sizeof(pAux->pMod->xClose_signature)) == 0) {
        rc = simpleClose(pCur->pChild);
      }
    else
      if (memcmp(pAux->pMod->xClose_signature, xClose_signatures[xClose_statClose_enum], sizeof(pAux->pMod->xClose_signature)) == 0) {
        rc = statClose(pCur->pChild);
      }
    else
      if (memcmp(pAux->pMod->xClose_signature, xClose_signatures[xClose_stmtClose_enum], sizeof(pAux->pMod->xClose_signature)) == 0) {
        rc = stmtClose(pCur->pChild);
      }
    else
      if (memcmp(pAux->pMod->xClose_signature, xClose_signatures[xClose_unicodeClose_enum], sizeof(pAux->pMod->xClose_signature)) == 0) {
        rc = unicodeClose(pCur->pChild);
      }
    else
      if (memcmp(pAux->pMod->xClose_signature, xClose_signatures[xClose_vfstraceClose_enum], sizeof(pAux->pMod->xClose_signature)) == 0) {
        rc = vfstraceClose(pCur->pChild);
      }
    else
      if (memcmp(pAux->pMod->xClose_signature, xClose_signatures[xClose_zipfileClose_enum], sizeof(pAux->pMod->xClose_signature)) == 0) {
        rc = zipfileClose(pCur->pChild);
      }
    else
      if (memcmp(pAux->pMod->xClose_signature, xClose_signatures[xClose_unixClose_enum], sizeof(pAux->pMod->xClose_signature)) == 0) {
        rc = unixClose(pCur->pChild);
      }
    else
      if (memcmp(pAux->pMod->xClose_signature, xClose_signatures[xClose_nolockClose_enum], sizeof(pAux->pMod->xClose_signature)) == 0) {
        rc = nolockClose(pCur->pChild);
      }
    else
      if (memcmp(pAux->pMod->xClose_signature, xClose_signatures[xClose_dotlockClose_enum], sizeof(pAux->pMod->xClose_signature)) == 0) {
        rc = dotlockClose(pCur->pChild);
      }
    if( rc!=SQLITE_OK ){
      VTSHIM_COPY_ERRMSG();
    }
  }
  if( pCur->pNext ) pCur->pNext->ppPrev = pCur->ppPrev;
  *pCur->ppPrev = pCur->pNext;
  sqlite3_free(pCur);
  return rc;
}

static int vtshimFilter(
  sqlite3_vtab_cursor *pX,
  int idxNum,
  const char *idxStr,
  int argc,
  sqlite3_value **argv
){
  vtshim_cursor *pCur = (vtshim_cursor*)pX;
  vtshim_vtab *pVtab = (vtshim_vtab*)pCur->base.pVtab;
  vtshim_aux *pAux = pVtab->pAux;
  int rc;
  if( pAux->bDisposed ) return SQLITE_ERROR;
  if (memcmp(pAux->pMod->xFilter_signature, xFilter_signatures[xFilter_amatchFilter_enum], sizeof(pAux->pMod->xFilter_signature)) == 0) {
    rc = amatchFilter(pCur->pChild, idxNum, idxStr, argc, argv);
  }
  else
    if (memcmp(pAux->pMod->xFilter_signature, xFilter_signatures[xFilter_binfoFilter_enum], sizeof(pAux->pMod->xFilter_signature)) == 0) {
      rc = binfoFilter(pCur->pChild, idxNum, idxStr, argc, argv);
    }
  else
    if (memcmp(pAux->pMod->xFilter_signature, xFilter_signatures[xFilter_bytecodevtabFilter_enum], sizeof(pAux->pMod->xFilter_signature)) == 0) {
      rc = bytecodevtabFilter(pCur->pChild, idxNum, idxStr, argc, argv);
    }
  else
    if (memcmp(pAux->pMod->xFilter_signature, xFilter_signatures[xFilter_carrayFilter_enum], sizeof(pAux->pMod->xFilter_signature)) == 0) {
      rc = carrayFilter(pCur->pChild, idxNum, idxStr, argc, argv);
    }
  else
    if (memcmp(pAux->pMod->xFilter_signature, xFilter_signatures[xFilter_cidxFilter_enum], sizeof(pAux->pMod->xFilter_signature)) == 0) {
      rc = cidxFilter(pCur->pChild, idxNum, idxStr, argc, argv);
    }
  else
    if (memcmp(pAux->pMod->xFilter_signature, xFilter_signatures[xFilter_closureFilter_enum], sizeof(pAux->pMod->xFilter_signature)) == 0) {
      rc = closureFilter(pCur->pChild, idxNum, idxStr, argc, argv);
    }
  else
    if (memcmp(pAux->pMod->xFilter_signature, xFilter_signatures[xFilter_completionFilter_enum], sizeof(pAux->pMod->xFilter_signature)) == 0) {
      rc = completionFilter(pCur->pChild, idxNum, idxStr, argc, argv);
    }
  else
    if (memcmp(pAux->pMod->xFilter_signature, xFilter_signatures[xFilter_csvtabFilter_enum], sizeof(pAux->pMod->xFilter_signature)) == 0) {
      rc = csvtabFilter(pCur->pChild, idxNum, idxStr, argc, argv);
    }
  else
    if (memcmp(pAux->pMod->xFilter_signature, xFilter_signatures[xFilter_dbdataFilter_enum], sizeof(pAux->pMod->xFilter_signature)) == 0) {
      rc = dbdataFilter(pCur->pChild, idxNum, idxStr, argc, argv);
    }
  else
    if (memcmp(pAux->pMod->xFilter_signature, xFilter_signatures[xFilter_dbpageFilter_enum], sizeof(pAux->pMod->xFilter_signature)) == 0) {
      rc = dbpageFilter(pCur->pChild, idxNum, idxStr, argc, argv);
    }
  else
    if (memcmp(pAux->pMod->xFilter_signature, xFilter_signatures[xFilter_deltaparsevtabFilter_enum], sizeof(pAux->pMod->xFilter_signature)) == 0) {
      rc = deltaparsevtabFilter(pCur->pChild, idxNum, idxStr, argc, argv);
    }
  else
    if (memcmp(pAux->pMod->xFilter_signature, xFilter_signatures[xFilter_echoFilter_enum], sizeof(pAux->pMod->xFilter_signature)) == 0) {
      rc = echoFilter(pCur->pChild, idxNum, idxStr, argc, argv);
    }
  else
    if (memcmp(pAux->pMod->xFilter_signature, xFilter_signatures[xFilter_expertFilter_enum], sizeof(pAux->pMod->xFilter_signature)) == 0) {
      rc = expertFilter(pCur->pChild, idxNum, idxStr, argc, argv);
    }
  else
    if (memcmp(pAux->pMod->xFilter_signature, xFilter_signatures[xFilter_explainFilter_enum], sizeof(pAux->pMod->xFilter_signature)) == 0) {
      rc = explainFilter(pCur->pChild, idxNum, idxStr, argc, argv);
    }
  else
    if (memcmp(pAux->pMod->xFilter_signature, xFilter_signatures[xFilter_fsFilter_enum], sizeof(pAux->pMod->xFilter_signature)) == 0) {
      rc = fsFilter(pCur->pChild, idxNum, idxStr, argc, argv);
    }
  else
    if (memcmp(pAux->pMod->xFilter_signature, xFilter_signatures[xFilter_fsdirFilter_enum], sizeof(pAux->pMod->xFilter_signature)) == 0) {
      rc = fsdirFilter(pCur->pChild, idxNum, idxStr, argc, argv);
    }
  else
    if (memcmp(pAux->pMod->xFilter_signature, xFilter_signatures[xFilter_fstreeFilter_enum], sizeof(pAux->pMod->xFilter_signature)) == 0) {
      rc = fstreeFilter(pCur->pChild, idxNum, idxStr, argc, argv);
    }
  else
    if (memcmp(pAux->pMod->xFilter_signature, xFilter_signatures[xFilter_fts3FilterMethod_enum], sizeof(pAux->pMod->xFilter_signature)) == 0) {
      rc = fts3FilterMethod(pCur->pChild, idxNum, idxStr, argc, argv);
    }
  else
    if (memcmp(pAux->pMod->xFilter_signature, xFilter_signatures[xFilter_fts3auxFilterMethod_enum], sizeof(pAux->pMod->xFilter_signature)) == 0) {
      rc = fts3auxFilterMethod(pCur->pChild, idxNum, idxStr, argc, argv);
    }
  else
    if (memcmp(pAux->pMod->xFilter_signature, xFilter_signatures[xFilter_fts3termFilterMethod_enum], sizeof(pAux->pMod->xFilter_signature)) == 0) {
      rc = fts3termFilterMethod(pCur->pChild, idxNum, idxStr, argc, argv);
    }
  else
    if (memcmp(pAux->pMod->xFilter_signature, xFilter_signatures[xFilter_fts3tokFilterMethod_enum], sizeof(pAux->pMod->xFilter_signature)) == 0) {
      rc = fts3tokFilterMethod(pCur->pChild, idxNum, idxStr, argc, argv);
    }
  else
    if (memcmp(pAux->pMod->xFilter_signature, xFilter_signatures[xFilter_fuzzerFilter_enum], sizeof(pAux->pMod->xFilter_signature)) == 0) {
      rc = fuzzerFilter(pCur->pChild, idxNum, idxStr, argc, argv);
    }
  else
    if (memcmp(pAux->pMod->xFilter_signature, xFilter_signatures[xFilter_geopolyFilter_enum], sizeof(pAux->pMod->xFilter_signature)) == 0) {
      rc = geopolyFilter(pCur->pChild, idxNum, idxStr, argc, argv);
    }
  else
    if (memcmp(pAux->pMod->xFilter_signature, xFilter_signatures[xFilter_intarrayFilter_enum], sizeof(pAux->pMod->xFilter_signature)) == 0) {
      rc = intarrayFilter(pCur->pChild, idxNum, idxStr, argc, argv);
    }
  else
    if (memcmp(pAux->pMod->xFilter_signature, xFilter_signatures[xFilter_jsonEachFilter_enum], sizeof(pAux->pMod->xFilter_signature)) == 0) {
      rc = jsonEachFilter(pCur->pChild, idxNum, idxStr, argc, argv);
    }
  else
    if (memcmp(pAux->pMod->xFilter_signature, xFilter_signatures[xFilter_memstatFilter_enum], sizeof(pAux->pMod->xFilter_signature)) == 0) {
      rc = memstatFilter(pCur->pChild, idxNum, idxStr, argc, argv);
    }
  else
    if (memcmp(pAux->pMod->xFilter_signature, xFilter_signatures[xFilter_pragmaVtabFilter_enum], sizeof(pAux->pMod->xFilter_signature)) == 0) {
      rc = pragmaVtabFilter(pCur->pChild, idxNum, idxStr, argc, argv);
    }
  else
    if (memcmp(pAux->pMod->xFilter_signature, xFilter_signatures[xFilter_prefixesFilter_enum], sizeof(pAux->pMod->xFilter_signature)) == 0) {
      rc = prefixesFilter(pCur->pChild, idxNum, idxStr, argc, argv);
    }
  else
    if (memcmp(pAux->pMod->xFilter_signature, xFilter_signatures[xFilter_qpvtabFilter_enum], sizeof(pAux->pMod->xFilter_signature)) == 0) {
      rc = qpvtabFilter(pCur->pChild, idxNum, idxStr, argc, argv);
    }
  else
    if (memcmp(pAux->pMod->xFilter_signature, xFilter_signatures[xFilter_rtreeFilter_enum], sizeof(pAux->pMod->xFilter_signature)) == 0) {
      rc = rtreeFilter(pCur->pChild, idxNum, idxStr, argc, argv);
    }
  else
    if (memcmp(pAux->pMod->xFilter_signature, xFilter_signatures[xFilter_schemaFilter_enum], sizeof(pAux->pMod->xFilter_signature)) == 0) {
      rc = schemaFilter(pCur->pChild, idxNum, idxStr, argc, argv);
    }
  else
    if (memcmp(pAux->pMod->xFilter_signature, xFilter_signatures[xFilter_seriesFilter_enum], sizeof(pAux->pMod->xFilter_signature)) == 0) {
      rc = seriesFilter(pCur->pChild, idxNum, idxStr, argc, argv);
    }
  else
    if (memcmp(pAux->pMod->xFilter_signature, xFilter_signatures[xFilter_spellfix1Filter_enum], sizeof(pAux->pMod->xFilter_signature)) == 0) {
      rc = spellfix1Filter(pCur->pChild, idxNum, idxStr, argc, argv);
    }
  else
    if (memcmp(pAux->pMod->xFilter_signature, xFilter_signatures[xFilter_statFilter_enum], sizeof(pAux->pMod->xFilter_signature)) == 0) {
      rc = statFilter(pCur->pChild, idxNum, idxStr, argc, argv);
    }
  else
    if (memcmp(pAux->pMod->xFilter_signature, xFilter_signatures[xFilter_stmtFilter_enum], sizeof(pAux->pMod->xFilter_signature)) == 0) {
      rc = stmtFilter(pCur->pChild, idxNum, idxStr, argc, argv);
    }
  else
    if (memcmp(pAux->pMod->xFilter_signature, xFilter_signatures[xFilter_tclFilter_enum], sizeof(pAux->pMod->xFilter_signature)) == 0) {
      rc = tclFilter(pCur->pChild, idxNum, idxStr, argc, argv);
    }
  else
    if (memcmp(pAux->pMod->xFilter_signature, xFilter_signatures[xFilter_tclvarFilter_enum], sizeof(pAux->pMod->xFilter_signature)) == 0) {
      rc = tclvarFilter(pCur->pChild, idxNum, idxStr, argc, argv);
    }
  else
    if (memcmp(pAux->pMod->xFilter_signature, xFilter_signatures[xFilter_templatevtabFilter_enum], sizeof(pAux->pMod->xFilter_signature)) == 0) {
      rc = templatevtabFilter(pCur->pChild, idxNum, idxStr, argc, argv);
    }
  else
    if (memcmp(pAux->pMod->xFilter_signature, xFilter_signatures[xFilter_unionFilter_enum], sizeof(pAux->pMod->xFilter_signature)) == 0) {
      rc = unionFilter(pCur->pChild, idxNum, idxStr, argc, argv);
    }
  else
    if (memcmp(pAux->pMod->xFilter_signature, xFilter_signatures[xFilter_vlogFilter_enum], sizeof(pAux->pMod->xFilter_signature)) == 0) {
      rc = vlogFilter(pCur->pChild, idxNum, idxStr, argc, argv);
    }
  else
    if (memcmp(pAux->pMod->xFilter_signature, xFilter_signatures[xFilter_vstattabFilter_enum], sizeof(pAux->pMod->xFilter_signature)) == 0) {
      rc = vstattabFilter(pCur->pChild, idxNum, idxStr, argc, argv);
    }
  else
    if (memcmp(pAux->pMod->xFilter_signature, xFilter_signatures[xFilter_vtablogFilter_enum], sizeof(pAux->pMod->xFilter_signature)) == 0) {
      rc = vtablogFilter(pCur->pChild, idxNum, idxStr, argc, argv);
    }
  else
    if (memcmp(pAux->pMod->xFilter_signature, xFilter_signatures[xFilter_wholenumberFilter_enum], sizeof(pAux->pMod->xFilter_signature)) == 0) {
      rc = wholenumberFilter(pCur->pChild, idxNum, idxStr, argc, argv);
    }
  else
    if (memcmp(pAux->pMod->xFilter_signature, xFilter_signatures[xFilter_zipfileFilter_enum], sizeof(pAux->pMod->xFilter_signature)) == 0) {
      rc = zipfileFilter(pCur->pChild, idxNum, idxStr, argc, argv);
    }
  if( rc!=SQLITE_OK ){
    VTSHIM_COPY_ERRMSG();
  }
  return rc;
}

static int vtshimNext(sqlite3_vtab_cursor *pX){
  vtshim_cursor *pCur = (vtshim_cursor*)pX;
  vtshim_vtab *pVtab = (vtshim_vtab*)pCur->base.pVtab;
  vtshim_aux *pAux = pVtab->pAux;
  int rc;
  if( pAux->bDisposed ) return SQLITE_ERROR;
  if (memcmp(pAux->pMod->xNext_signature, xNext_signatures[xNext_0_enum], sizeof(pAux->pMod->xNext_signature)) == 0) {
    rc = 0;
  }
  else
    if (memcmp(pAux->pMod->xNext_signature, xNext_signatures[xNext_amatchNext_enum], sizeof(pAux->pMod->xNext_signature)) == 0) {
      rc = amatchNext(pCur->pChild);
    }
  else
    if (memcmp(pAux->pMod->xNext_signature, xNext_signatures[xNext_binfoNext_enum], sizeof(pAux->pMod->xNext_signature)) == 0) {
      rc = binfoNext(pCur->pChild);
    }
  else
    if (memcmp(pAux->pMod->xNext_signature, xNext_signatures[xNext_bytecodevtabNext_enum], sizeof(pAux->pMod->xNext_signature)) == 0) {
      rc = bytecodevtabNext(pCur->pChild);
    }
  else
    if (memcmp(pAux->pMod->xNext_signature, xNext_signatures[xNext_carrayNext_enum], sizeof(pAux->pMod->xNext_signature)) == 0) {
      rc = carrayNext(pCur->pChild);
    }
  else
    if (memcmp(pAux->pMod->xNext_signature, xNext_signatures[xNext_cidxNext_enum], sizeof(pAux->pMod->xNext_signature)) == 0) {
      rc = cidxNext(pCur->pChild);
    }
  else
    if (memcmp(pAux->pMod->xNext_signature, xNext_signatures[xNext_closureNext_enum], sizeof(pAux->pMod->xNext_signature)) == 0) {
      rc = closureNext(pCur->pChild);
    }
  else
    if (memcmp(pAux->pMod->xNext_signature, xNext_signatures[xNext_completionNext_enum], sizeof(pAux->pMod->xNext_signature)) == 0) {
      rc = completionNext(pCur->pChild);
    }
  else
    if (memcmp(pAux->pMod->xNext_signature, xNext_signatures[xNext_csvtabNext_enum], sizeof(pAux->pMod->xNext_signature)) == 0) {
      rc = csvtabNext(pCur->pChild);
    }
  else
    if (memcmp(pAux->pMod->xNext_signature, xNext_signatures[xNext_dbdataNext_enum], sizeof(pAux->pMod->xNext_signature)) == 0) {
      rc = dbdataNext(pCur->pChild);
    }
  else
    if (memcmp(pAux->pMod->xNext_signature, xNext_signatures[xNext_dbpageNext_enum], sizeof(pAux->pMod->xNext_signature)) == 0) {
      rc = dbpageNext(pCur->pChild);
    }
  else
    if (memcmp(pAux->pMod->xNext_signature, xNext_signatures[xNext_deltaparsevtabNext_enum], sizeof(pAux->pMod->xNext_signature)) == 0) {
      rc = deltaparsevtabNext(pCur->pChild);
    }
  else
    if (memcmp(pAux->pMod->xNext_signature, xNext_signatures[xNext_echoNext_enum], sizeof(pAux->pMod->xNext_signature)) == 0) {
      rc = echoNext(pCur->pChild);
    }
  else
    if (memcmp(pAux->pMod->xNext_signature, xNext_signatures[xNext_expertNext_enum], sizeof(pAux->pMod->xNext_signature)) == 0) {
      rc = expertNext(pCur->pChild);
    }
  else
    if (memcmp(pAux->pMod->xNext_signature, xNext_signatures[xNext_explainNext_enum], sizeof(pAux->pMod->xNext_signature)) == 0) {
      rc = explainNext(pCur->pChild);
    }
  else
    if (memcmp(pAux->pMod->xNext_signature, xNext_signatures[xNext_fsNext_enum], sizeof(pAux->pMod->xNext_signature)) == 0) {
      rc = fsNext(pCur->pChild);
    }
  else
    if (memcmp(pAux->pMod->xNext_signature, xNext_signatures[xNext_fsdirNext_enum], sizeof(pAux->pMod->xNext_signature)) == 0) {
      rc = fsdirNext(pCur->pChild);
    }
  else
    if (memcmp(pAux->pMod->xNext_signature, xNext_signatures[xNext_fstreeNext_enum], sizeof(pAux->pMod->xNext_signature)) == 0) {
      rc = fstreeNext(pCur->pChild);
    }
  else
    if (memcmp(pAux->pMod->xNext_signature, xNext_signatures[xNext_fts3NextMethod_enum], sizeof(pAux->pMod->xNext_signature)) == 0) {
      rc = fts3NextMethod(pCur->pChild);
    }
  else
    if (memcmp(pAux->pMod->xNext_signature, xNext_signatures[xNext_fts3auxNextMethod_enum], sizeof(pAux->pMod->xNext_signature)) == 0) {
      rc = fts3auxNextMethod(pCur->pChild);
    }
  else
    if (memcmp(pAux->pMod->xNext_signature, xNext_signatures[xNext_fts3termNextMethod_enum], sizeof(pAux->pMod->xNext_signature)) == 0) {
      rc = fts3termNextMethod(pCur->pChild);
    }
  else
    if (memcmp(pAux->pMod->xNext_signature, xNext_signatures[xNext_fts3tokNextMethod_enum], sizeof(pAux->pMod->xNext_signature)) == 0) {
      rc = fts3tokNextMethod(pCur->pChild);
    }
  else
    if (memcmp(pAux->pMod->xNext_signature, xNext_signatures[xNext_fuzzerNext_enum], sizeof(pAux->pMod->xNext_signature)) == 0) {
      rc = fuzzerNext(pCur->pChild);
    }
  else
    if (memcmp(pAux->pMod->xNext_signature, xNext_signatures[xNext_intarrayNext_enum], sizeof(pAux->pMod->xNext_signature)) == 0) {
      rc = intarrayNext(pCur->pChild);
    }
  else
    if (memcmp(pAux->pMod->xNext_signature, xNext_signatures[xNext_jsonEachNext_enum], sizeof(pAux->pMod->xNext_signature)) == 0) {
      rc = jsonEachNext(pCur->pChild);
    }
  else
    if (memcmp(pAux->pMod->xNext_signature, xNext_signatures[xNext_memstatNext_enum], sizeof(pAux->pMod->xNext_signature)) == 0) {
      rc = memstatNext(pCur->pChild);
    }
  else
    if (memcmp(pAux->pMod->xNext_signature, xNext_signatures[xNext_porterNext_enum], sizeof(pAux->pMod->xNext_signature)) == 0) {
      rc = porterNext(pCur->pChild);
    }
  else
    if (memcmp(pAux->pMod->xNext_signature, xNext_signatures[xNext_pragmaVtabNext_enum], sizeof(pAux->pMod->xNext_signature)) == 0) {
      rc = pragmaVtabNext(pCur->pChild);
    }
  else
    if (memcmp(pAux->pMod->xNext_signature, xNext_signatures[xNext_prefixesNext_enum], sizeof(pAux->pMod->xNext_signature)) == 0) {
      rc = prefixesNext(pCur->pChild);
    }
  else
    if (memcmp(pAux->pMod->xNext_signature, xNext_signatures[xNext_qpvtabNext_enum], sizeof(pAux->pMod->xNext_signature)) == 0) {
      rc = qpvtabNext(pCur->pChild);
    }
  else
    if (memcmp(pAux->pMod->xNext_signature, xNext_signatures[xNext_rtreeNext_enum], sizeof(pAux->pMod->xNext_signature)) == 0) {
      rc = rtreeNext(pCur->pChild);
    }
  else
    if (memcmp(pAux->pMod->xNext_signature, xNext_signatures[xNext_schemaNext_enum], sizeof(pAux->pMod->xNext_signature)) == 0) {
      rc = schemaNext(pCur->pChild);
    }
  else
    if (memcmp(pAux->pMod->xNext_signature, xNext_signatures[xNext_seriesNext_enum], sizeof(pAux->pMod->xNext_signature)) == 0) {
      rc = seriesNext(pCur->pChild);
    }
  else
    if (memcmp(pAux->pMod->xNext_signature, xNext_signatures[xNext_simpleNext_enum], sizeof(pAux->pMod->xNext_signature)) == 0) {
      rc = simpleNext(pCur->pChild);
    }
  else
    if (memcmp(pAux->pMod->xNext_signature, xNext_signatures[xNext_spellfix1Next_enum], sizeof(pAux->pMod->xNext_signature)) == 0) {
      rc = spellfix1Next(pCur->pChild);
    }
  else
    if (memcmp(pAux->pMod->xNext_signature, xNext_signatures[xNext_statNext_enum], sizeof(pAux->pMod->xNext_signature)) == 0) {
      rc = statNext(pCur->pChild);
    }
  else
    if (memcmp(pAux->pMod->xNext_signature, xNext_signatures[xNext_stmtNext_enum], sizeof(pAux->pMod->xNext_signature)) == 0) {
      rc = stmtNext(pCur->pChild);
    }
  else
    if (memcmp(pAux->pMod->xNext_signature, xNext_signatures[xNext_tclNext_enum], sizeof(pAux->pMod->xNext_signature)) == 0) {
      rc = tclNext(pCur->pChild);
    }
  else
    if (memcmp(pAux->pMod->xNext_signature, xNext_signatures[xNext_tclvarNext_enum], sizeof(pAux->pMod->xNext_signature)) == 0) {
      rc = tclvarNext(pCur->pChild);
    }
  else
    if (memcmp(pAux->pMod->xNext_signature, xNext_signatures[xNext_templatevtabNext_enum], sizeof(pAux->pMod->xNext_signature)) == 0) {
      rc = templatevtabNext(pCur->pChild);
    }
  else
    if (memcmp(pAux->pMod->xNext_signature, xNext_signatures[xNext_unicodeNext_enum], sizeof(pAux->pMod->xNext_signature)) == 0) {
      rc = unicodeNext(pCur->pChild);
    }
  else
    if (memcmp(pAux->pMod->xNext_signature, xNext_signatures[xNext_unionNext_enum], sizeof(pAux->pMod->xNext_signature)) == 0) {
      rc = unionNext(pCur->pChild);
    }
  else
    if (memcmp(pAux->pMod->xNext_signature, xNext_signatures[xNext_vlogNext_enum], sizeof(pAux->pMod->xNext_signature)) == 0) {
      rc = vlogNext(pCur->pChild);
    }
  else
    if (memcmp(pAux->pMod->xNext_signature, xNext_signatures[xNext_vstattabNext_enum], sizeof(pAux->pMod->xNext_signature)) == 0) {
      rc = vstattabNext(pCur->pChild);
    }
  else
    if (memcmp(pAux->pMod->xNext_signature, xNext_signatures[xNext_vtablogNext_enum], sizeof(pAux->pMod->xNext_signature)) == 0) {
      rc = vtablogNext(pCur->pChild);
    }
  else
    if (memcmp(pAux->pMod->xNext_signature, xNext_signatures[xNext_wholenumberNext_enum], sizeof(pAux->pMod->xNext_signature)) == 0) {
      rc = wholenumberNext(pCur->pChild);
    }
  else
    if (memcmp(pAux->pMod->xNext_signature, xNext_signatures[xNext_zipfileNext_enum], sizeof(pAux->pMod->xNext_signature)) == 0) {
      rc = zipfileNext(pCur->pChild);
    }
  if( rc!=SQLITE_OK ){
    VTSHIM_COPY_ERRMSG();
  }
  return rc;
}

static int vtshimEof(sqlite3_vtab_cursor *pX){
  vtshim_cursor *pCur = (vtshim_cursor*)pX;
  vtshim_vtab *pVtab = (vtshim_vtab*)pCur->base.pVtab;
  vtshim_aux *pAux = pVtab->pAux;
  int rc;
  if( pAux->bDisposed ) return 1;
  if (memcmp(pAux->pMod->xEof_signature, xEof_signatures[xEof_amatchEof_enum], sizeof(pAux->pMod->xEof_signature)) == 0) {
    rc = amatchEof(pCur->pChild);
  }
  else
    if (memcmp(pAux->pMod->xEof_signature, xEof_signatures[xEof_binfoEof_enum], sizeof(pAux->pMod->xEof_signature)) == 0) {
      rc = binfoEof(pCur->pChild);
    }
  else
    if (memcmp(pAux->pMod->xEof_signature, xEof_signatures[xEof_bytecodevtabEof_enum], sizeof(pAux->pMod->xEof_signature)) == 0) {
      rc = bytecodevtabEof(pCur->pChild);
    }
  else
    if (memcmp(pAux->pMod->xEof_signature, xEof_signatures[xEof_carrayEof_enum], sizeof(pAux->pMod->xEof_signature)) == 0) {
      rc = carrayEof(pCur->pChild);
    }
  else
    if (memcmp(pAux->pMod->xEof_signature, xEof_signatures[xEof_cidxEof_enum], sizeof(pAux->pMod->xEof_signature)) == 0) {
      rc = cidxEof(pCur->pChild);
    }
  else
    if (memcmp(pAux->pMod->xEof_signature, xEof_signatures[xEof_closureEof_enum], sizeof(pAux->pMod->xEof_signature)) == 0) {
      rc = closureEof(pCur->pChild);
    }
  else
    if (memcmp(pAux->pMod->xEof_signature, xEof_signatures[xEof_completionEof_enum], sizeof(pAux->pMod->xEof_signature)) == 0) {
      rc = completionEof(pCur->pChild);
    }
  else
    if (memcmp(pAux->pMod->xEof_signature, xEof_signatures[xEof_csvtabEof_enum], sizeof(pAux->pMod->xEof_signature)) == 0) {
      rc = csvtabEof(pCur->pChild);
    }
  else
    if (memcmp(pAux->pMod->xEof_signature, xEof_signatures[xEof_dbdataEof_enum], sizeof(pAux->pMod->xEof_signature)) == 0) {
      rc = dbdataEof(pCur->pChild);
    }
  else
    if (memcmp(pAux->pMod->xEof_signature, xEof_signatures[xEof_dbpageEof_enum], sizeof(pAux->pMod->xEof_signature)) == 0) {
      rc = dbpageEof(pCur->pChild);
    }
  else
    if (memcmp(pAux->pMod->xEof_signature, xEof_signatures[xEof_deltaparsevtabEof_enum], sizeof(pAux->pMod->xEof_signature)) == 0) {
      rc = deltaparsevtabEof(pCur->pChild);
    }
  else
    if (memcmp(pAux->pMod->xEof_signature, xEof_signatures[xEof_echoEof_enum], sizeof(pAux->pMod->xEof_signature)) == 0) {
      rc = echoEof(pCur->pChild);
    }
  else
    if (memcmp(pAux->pMod->xEof_signature, xEof_signatures[xEof_expertEof_enum], sizeof(pAux->pMod->xEof_signature)) == 0) {
      rc = expertEof(pCur->pChild);
    }
  else
    if (memcmp(pAux->pMod->xEof_signature, xEof_signatures[xEof_explainEof_enum], sizeof(pAux->pMod->xEof_signature)) == 0) {
      rc = explainEof(pCur->pChild);
    }
  else
    if (memcmp(pAux->pMod->xEof_signature, xEof_signatures[xEof_fsEof_enum], sizeof(pAux->pMod->xEof_signature)) == 0) {
      rc = fsEof(pCur->pChild);
    }
  else
    if (memcmp(pAux->pMod->xEof_signature, xEof_signatures[xEof_fsdirEof_enum], sizeof(pAux->pMod->xEof_signature)) == 0) {
      rc = fsdirEof(pCur->pChild);
    }
  else
    if (memcmp(pAux->pMod->xEof_signature, xEof_signatures[xEof_fstreeEof_enum], sizeof(pAux->pMod->xEof_signature)) == 0) {
      rc = fstreeEof(pCur->pChild);
    }
  else
    if (memcmp(pAux->pMod->xEof_signature, xEof_signatures[xEof_fts3EofMethod_enum], sizeof(pAux->pMod->xEof_signature)) == 0) {
      rc = fts3EofMethod(pCur->pChild);
    }
  else
    if (memcmp(pAux->pMod->xEof_signature, xEof_signatures[xEof_fts3auxEofMethod_enum], sizeof(pAux->pMod->xEof_signature)) == 0) {
      rc = fts3auxEofMethod(pCur->pChild);
    }
  else
    if (memcmp(pAux->pMod->xEof_signature, xEof_signatures[xEof_fts3termEofMethod_enum], sizeof(pAux->pMod->xEof_signature)) == 0) {
      rc = fts3termEofMethod(pCur->pChild);
    }
  else
    if (memcmp(pAux->pMod->xEof_signature, xEof_signatures[xEof_fts3tokEofMethod_enum], sizeof(pAux->pMod->xEof_signature)) == 0) {
      rc = fts3tokEofMethod(pCur->pChild);
    }
  else
    if (memcmp(pAux->pMod->xEof_signature, xEof_signatures[xEof_fuzzerEof_enum], sizeof(pAux->pMod->xEof_signature)) == 0) {
      rc = fuzzerEof(pCur->pChild);
    }
  else
    if (memcmp(pAux->pMod->xEof_signature, xEof_signatures[xEof_intarrayEof_enum], sizeof(pAux->pMod->xEof_signature)) == 0) {
      rc = intarrayEof(pCur->pChild);
    }
  else
    if (memcmp(pAux->pMod->xEof_signature, xEof_signatures[xEof_jsonEachEof_enum], sizeof(pAux->pMod->xEof_signature)) == 0) {
      rc = jsonEachEof(pCur->pChild);
    }
  else
    if (memcmp(pAux->pMod->xEof_signature, xEof_signatures[xEof_memstatEof_enum], sizeof(pAux->pMod->xEof_signature)) == 0) {
      rc = memstatEof(pCur->pChild);
    }
  else
    if (memcmp(pAux->pMod->xEof_signature, xEof_signatures[xEof_pragmaVtabEof_enum], sizeof(pAux->pMod->xEof_signature)) == 0) {
      rc = pragmaVtabEof(pCur->pChild);
    }
  else
    if (memcmp(pAux->pMod->xEof_signature, xEof_signatures[xEof_prefixesEof_enum], sizeof(pAux->pMod->xEof_signature)) == 0) {
      rc = prefixesEof(pCur->pChild);
    }
  else
    if (memcmp(pAux->pMod->xEof_signature, xEof_signatures[xEof_qpvtabEof_enum], sizeof(pAux->pMod->xEof_signature)) == 0) {
      rc = qpvtabEof(pCur->pChild);
    }
  else
    if (memcmp(pAux->pMod->xEof_signature, xEof_signatures[xEof_rtreeEof_enum], sizeof(pAux->pMod->xEof_signature)) == 0) {
      rc = rtreeEof(pCur->pChild);
    }
  else
    if (memcmp(pAux->pMod->xEof_signature, xEof_signatures[xEof_schemaEof_enum], sizeof(pAux->pMod->xEof_signature)) == 0) {
      rc = schemaEof(pCur->pChild);
    }
  else
    if (memcmp(pAux->pMod->xEof_signature, xEof_signatures[xEof_seriesEof_enum], sizeof(pAux->pMod->xEof_signature)) == 0) {
      rc = seriesEof(pCur->pChild);
    }
  else
    if (memcmp(pAux->pMod->xEof_signature, xEof_signatures[xEof_spellfix1Eof_enum], sizeof(pAux->pMod->xEof_signature)) == 0) {
      rc = spellfix1Eof(pCur->pChild);
    }
  else
    if (memcmp(pAux->pMod->xEof_signature, xEof_signatures[xEof_statEof_enum], sizeof(pAux->pMod->xEof_signature)) == 0) {
      rc = statEof(pCur->pChild);
    }
  else
    if (memcmp(pAux->pMod->xEof_signature, xEof_signatures[xEof_stmtEof_enum], sizeof(pAux->pMod->xEof_signature)) == 0) {
      rc = stmtEof(pCur->pChild);
    }
  else
    if (memcmp(pAux->pMod->xEof_signature, xEof_signatures[xEof_tclEof_enum], sizeof(pAux->pMod->xEof_signature)) == 0) {
      rc = tclEof(pCur->pChild);
    }
  else
    if (memcmp(pAux->pMod->xEof_signature, xEof_signatures[xEof_tclvarEof_enum], sizeof(pAux->pMod->xEof_signature)) == 0) {
      rc = tclvarEof(pCur->pChild);
    }
  else
    if (memcmp(pAux->pMod->xEof_signature, xEof_signatures[xEof_templatevtabEof_enum], sizeof(pAux->pMod->xEof_signature)) == 0) {
      rc = templatevtabEof(pCur->pChild);
    }
  else
    if (memcmp(pAux->pMod->xEof_signature, xEof_signatures[xEof_unionEof_enum], sizeof(pAux->pMod->xEof_signature)) == 0) {
      rc = unionEof(pCur->pChild);
    }
  else
    if (memcmp(pAux->pMod->xEof_signature, xEof_signatures[xEof_vlogEof_enum], sizeof(pAux->pMod->xEof_signature)) == 0) {
      rc = vlogEof(pCur->pChild);
    }
  else
    if (memcmp(pAux->pMod->xEof_signature, xEof_signatures[xEof_vstattabEof_enum], sizeof(pAux->pMod->xEof_signature)) == 0) {
      rc = vstattabEof(pCur->pChild);
    }
  else
    if (memcmp(pAux->pMod->xEof_signature, xEof_signatures[xEof_vtablogEof_enum], sizeof(pAux->pMod->xEof_signature)) == 0) {
      rc = vtablogEof(pCur->pChild);
    }
  else
    if (memcmp(pAux->pMod->xEof_signature, xEof_signatures[xEof_wholenumberEof_enum], sizeof(pAux->pMod->xEof_signature)) == 0) {
      rc = wholenumberEof(pCur->pChild);
    }
  else
    if (memcmp(pAux->pMod->xEof_signature, xEof_signatures[xEof_zipfileEof_enum], sizeof(pAux->pMod->xEof_signature)) == 0) {
      rc = zipfileEof(pCur->pChild);
    }
  VTSHIM_COPY_ERRMSG();
  return rc;
}

static int vtshimColumn(sqlite3_vtab_cursor *pX, sqlite3_context *ctx, int i){
  vtshim_cursor *pCur = (vtshim_cursor*)pX;
  vtshim_vtab *pVtab = (vtshim_vtab*)pCur->base.pVtab;
  vtshim_aux *pAux = pVtab->pAux;
  int rc;
  if( pAux->bDisposed ) return SQLITE_ERROR;
  if (memcmp(pAux->pMod->xColumn_signature, xColumn_signatures[xColumn_amatchColumn_enum], sizeof(pAux->pMod->xColumn_signature)) == 0) {
    rc = amatchColumn(pCur->pChild, ctx, i);
  }
  else
    if (memcmp(pAux->pMod->xColumn_signature, xColumn_signatures[xColumn_binfoColumn_enum], sizeof(pAux->pMod->xColumn_signature)) == 0) {
      rc = binfoColumn(pCur->pChild, ctx, i);
    }
  else
    if (memcmp(pAux->pMod->xColumn_signature, xColumn_signatures[xColumn_bytecodevtabColumn_enum], sizeof(pAux->pMod->xColumn_signature)) == 0) {
      rc = bytecodevtabColumn(pCur->pChild, ctx, i);
    }
  else
    if (memcmp(pAux->pMod->xColumn_signature, xColumn_signatures[xColumn_carrayColumn_enum], sizeof(pAux->pMod->xColumn_signature)) == 0) {
      rc = carrayColumn(pCur->pChild, ctx, i);
    }
  else
    if (memcmp(pAux->pMod->xColumn_signature, xColumn_signatures[xColumn_cidxColumn_enum], sizeof(pAux->pMod->xColumn_signature)) == 0) {
      rc = cidxColumn(pCur->pChild, ctx, i);
    }
  else
    if (memcmp(pAux->pMod->xColumn_signature, xColumn_signatures[xColumn_closureColumn_enum], sizeof(pAux->pMod->xColumn_signature)) == 0) {
      rc = closureColumn(pCur->pChild, ctx, i);
    }
  else
    if (memcmp(pAux->pMod->xColumn_signature, xColumn_signatures[xColumn_completionColumn_enum], sizeof(pAux->pMod->xColumn_signature)) == 0) {
      rc = completionColumn(pCur->pChild, ctx, i);
    }
  else
    if (memcmp(pAux->pMod->xColumn_signature, xColumn_signatures[xColumn_csvtabColumn_enum], sizeof(pAux->pMod->xColumn_signature)) == 0) {
      rc = csvtabColumn(pCur->pChild, ctx, i);
    }
  else
    if (memcmp(pAux->pMod->xColumn_signature, xColumn_signatures[xColumn_dbdataColumn_enum], sizeof(pAux->pMod->xColumn_signature)) == 0) {
      rc = dbdataColumn(pCur->pChild, ctx, i);
    }
  else
    if (memcmp(pAux->pMod->xColumn_signature, xColumn_signatures[xColumn_dbpageColumn_enum], sizeof(pAux->pMod->xColumn_signature)) == 0) {
      rc = dbpageColumn(pCur->pChild, ctx, i);
    }
  else
    if (memcmp(pAux->pMod->xColumn_signature, xColumn_signatures[xColumn_deltaparsevtabColumn_enum], sizeof(pAux->pMod->xColumn_signature)) == 0) {
      rc = deltaparsevtabColumn(pCur->pChild, ctx, i);
    }
  else
    if (memcmp(pAux->pMod->xColumn_signature, xColumn_signatures[xColumn_echoColumn_enum], sizeof(pAux->pMod->xColumn_signature)) == 0) {
      rc = echoColumn(pCur->pChild, ctx, i);
    }
  else
    if (memcmp(pAux->pMod->xColumn_signature, xColumn_signatures[xColumn_expertColumn_enum], sizeof(pAux->pMod->xColumn_signature)) == 0) {
      rc = expertColumn(pCur->pChild, ctx, i);
    }
  else
    if (memcmp(pAux->pMod->xColumn_signature, xColumn_signatures[xColumn_explainColumn_enum], sizeof(pAux->pMod->xColumn_signature)) == 0) {
      rc = explainColumn(pCur->pChild, ctx, i);
    }
  else
    if (memcmp(pAux->pMod->xColumn_signature, xColumn_signatures[xColumn_fsColumn_enum], sizeof(pAux->pMod->xColumn_signature)) == 0) {
      rc = fsColumn(pCur->pChild, ctx, i);
    }
  else
    if (memcmp(pAux->pMod->xColumn_signature, xColumn_signatures[xColumn_fsdirColumn_enum], sizeof(pAux->pMod->xColumn_signature)) == 0) {
      rc = fsdirColumn(pCur->pChild, ctx, i);
    }
  else
    if (memcmp(pAux->pMod->xColumn_signature, xColumn_signatures[xColumn_fstreeColumn_enum], sizeof(pAux->pMod->xColumn_signature)) == 0) {
      rc = fstreeColumn(pCur->pChild, ctx, i);
    }
  else
    if (memcmp(pAux->pMod->xColumn_signature, xColumn_signatures[xColumn_fts3ColumnMethod_enum], sizeof(pAux->pMod->xColumn_signature)) == 0) {
      rc = fts3ColumnMethod(pCur->pChild, ctx, i);
    }
  else
    if (memcmp(pAux->pMod->xColumn_signature, xColumn_signatures[xColumn_fts3auxColumnMethod_enum], sizeof(pAux->pMod->xColumn_signature)) == 0) {
      rc = fts3auxColumnMethod(pCur->pChild, ctx, i);
    }
  else
    if (memcmp(pAux->pMod->xColumn_signature, xColumn_signatures[xColumn_fts3termColumnMethod_enum], sizeof(pAux->pMod->xColumn_signature)) == 0) {
      rc = fts3termColumnMethod(pCur->pChild, ctx, i);
    }
  else
    if (memcmp(pAux->pMod->xColumn_signature, xColumn_signatures[xColumn_fts3tokColumnMethod_enum], sizeof(pAux->pMod->xColumn_signature)) == 0) {
      rc = fts3tokColumnMethod(pCur->pChild, ctx, i);
    }
  else
    if (memcmp(pAux->pMod->xColumn_signature, xColumn_signatures[xColumn_fuzzerColumn_enum], sizeof(pAux->pMod->xColumn_signature)) == 0) {
      rc = fuzzerColumn(pCur->pChild, ctx, i);
    }
  else
    if (memcmp(pAux->pMod->xColumn_signature, xColumn_signatures[xColumn_geopolyColumn_enum], sizeof(pAux->pMod->xColumn_signature)) == 0) {
      rc = geopolyColumn(pCur->pChild, ctx, i);
    }
  else
    if (memcmp(pAux->pMod->xColumn_signature, xColumn_signatures[xColumn_intarrayColumn_enum], sizeof(pAux->pMod->xColumn_signature)) == 0) {
      rc = intarrayColumn(pCur->pChild, ctx, i);
    }
  else
    if (memcmp(pAux->pMod->xColumn_signature, xColumn_signatures[xColumn_jsonEachColumn_enum], sizeof(pAux->pMod->xColumn_signature)) == 0) {
      rc = jsonEachColumn(pCur->pChild, ctx, i);
    }
  else
    if (memcmp(pAux->pMod->xColumn_signature, xColumn_signatures[xColumn_memstatColumn_enum], sizeof(pAux->pMod->xColumn_signature)) == 0) {
      rc = memstatColumn(pCur->pChild, ctx, i);
    }
  else
    if (memcmp(pAux->pMod->xColumn_signature, xColumn_signatures[xColumn_pragmaVtabColumn_enum], sizeof(pAux->pMod->xColumn_signature)) == 0) {
      rc = pragmaVtabColumn(pCur->pChild, ctx, i);
    }
  else
    if (memcmp(pAux->pMod->xColumn_signature, xColumn_signatures[xColumn_prefixesColumn_enum], sizeof(pAux->pMod->xColumn_signature)) == 0) {
      rc = prefixesColumn(pCur->pChild, ctx, i);
    }
  else
    if (memcmp(pAux->pMod->xColumn_signature, xColumn_signatures[xColumn_qpvtabColumn_enum], sizeof(pAux->pMod->xColumn_signature)) == 0) {
      rc = qpvtabColumn(pCur->pChild, ctx, i);
    }
  else
    if (memcmp(pAux->pMod->xColumn_signature, xColumn_signatures[xColumn_rtreeColumn_enum], sizeof(pAux->pMod->xColumn_signature)) == 0) {
      rc = rtreeColumn(pCur->pChild, ctx, i);
    }
  else
    if (memcmp(pAux->pMod->xColumn_signature, xColumn_signatures[xColumn_schemaColumn_enum], sizeof(pAux->pMod->xColumn_signature)) == 0) {
      rc = schemaColumn(pCur->pChild, ctx, i);
    }
  else
    if (memcmp(pAux->pMod->xColumn_signature, xColumn_signatures[xColumn_seriesColumn_enum], sizeof(pAux->pMod->xColumn_signature)) == 0) {
      rc = seriesColumn(pCur->pChild, ctx, i);
    }
  else
    if (memcmp(pAux->pMod->xColumn_signature, xColumn_signatures[xColumn_spellfix1Column_enum], sizeof(pAux->pMod->xColumn_signature)) == 0) {
      rc = spellfix1Column(pCur->pChild, ctx, i);
    }
  else
    if (memcmp(pAux->pMod->xColumn_signature, xColumn_signatures[xColumn_statColumn_enum], sizeof(pAux->pMod->xColumn_signature)) == 0) {
      rc = statColumn(pCur->pChild, ctx, i);
    }
  else
    if (memcmp(pAux->pMod->xColumn_signature, xColumn_signatures[xColumn_stmtColumn_enum], sizeof(pAux->pMod->xColumn_signature)) == 0) {
      rc = stmtColumn(pCur->pChild, ctx, i);
    }
  else
    if (memcmp(pAux->pMod->xColumn_signature, xColumn_signatures[xColumn_tclColumn_enum], sizeof(pAux->pMod->xColumn_signature)) == 0) {
      rc = tclColumn(pCur->pChild, ctx, i);
    }
  else
    if (memcmp(pAux->pMod->xColumn_signature, xColumn_signatures[xColumn_tclvarColumn_enum], sizeof(pAux->pMod->xColumn_signature)) == 0) {
      rc = tclvarColumn(pCur->pChild, ctx, i);
    }
  else
    if (memcmp(pAux->pMod->xColumn_signature, xColumn_signatures[xColumn_templatevtabColumn_enum], sizeof(pAux->pMod->xColumn_signature)) == 0) {
      rc = templatevtabColumn(pCur->pChild, ctx, i);
    }
  else
    if (memcmp(pAux->pMod->xColumn_signature, xColumn_signatures[xColumn_unionColumn_enum], sizeof(pAux->pMod->xColumn_signature)) == 0) {
      rc = unionColumn(pCur->pChild, ctx, i);
    }
  else
    if (memcmp(pAux->pMod->xColumn_signature, xColumn_signatures[xColumn_vlogColumn_enum], sizeof(pAux->pMod->xColumn_signature)) == 0) {
      rc = vlogColumn(pCur->pChild, ctx, i);
    }
  else
    if (memcmp(pAux->pMod->xColumn_signature, xColumn_signatures[xColumn_vstattabColumn_enum], sizeof(pAux->pMod->xColumn_signature)) == 0) {
      rc = vstattabColumn(pCur->pChild, ctx, i);
    }
  else
    if (memcmp(pAux->pMod->xColumn_signature, xColumn_signatures[xColumn_vtablogColumn_enum], sizeof(pAux->pMod->xColumn_signature)) == 0) {
      rc = vtablogColumn(pCur->pChild, ctx, i);
    }
  else
    if (memcmp(pAux->pMod->xColumn_signature, xColumn_signatures[xColumn_wholenumberColumn_enum], sizeof(pAux->pMod->xColumn_signature)) == 0) {
      rc = wholenumberColumn(pCur->pChild, ctx, i);
    }
  else
    if (memcmp(pAux->pMod->xColumn_signature, xColumn_signatures[xColumn_zipfileColumn_enum], sizeof(pAux->pMod->xColumn_signature)) == 0) {
      rc = zipfileColumn(pCur->pChild, ctx, i);
    }
  if( rc!=SQLITE_OK ){
    VTSHIM_COPY_ERRMSG();
  }
  return rc;
}

static int vtshimRowid(sqlite3_vtab_cursor *pX, sqlite3_int64 *pRowid){
  vtshim_cursor *pCur = (vtshim_cursor*)pX;
  vtshim_vtab *pVtab = (vtshim_vtab*)pCur->base.pVtab;
  vtshim_aux *pAux = pVtab->pAux;
  int rc;
  if( pAux->bDisposed ) return SQLITE_ERROR;
  if (memcmp(pAux->pMod->xRowid_signature, xRowid_signatures[xRowid_0_enum], sizeof(pAux->pMod->xRowid_signature)) == 0) {
    rc = 0;
  }
  else
    if (memcmp(pAux->pMod->xRowid_signature, xRowid_signatures[xRowid_amatchRowid_enum], sizeof(pAux->pMod->xRowid_signature)) == 0) {
      rc = amatchRowid(pCur->pChild, pRowid);
    }
  else
    if (memcmp(pAux->pMod->xRowid_signature, xRowid_signatures[xRowid_binfoRowid_enum], sizeof(pAux->pMod->xRowid_signature)) == 0) {
      rc = binfoRowid(pCur->pChild, pRowid);
    }
  else
    if (memcmp(pAux->pMod->xRowid_signature, xRowid_signatures[xRowid_bytecodevtabRowid_enum], sizeof(pAux->pMod->xRowid_signature)) == 0) {
      rc = bytecodevtabRowid(pCur->pChild, pRowid);
    }
  else
    if (memcmp(pAux->pMod->xRowid_signature, xRowid_signatures[xRowid_carrayRowid_enum], sizeof(pAux->pMod->xRowid_signature)) == 0) {
      rc = carrayRowid(pCur->pChild, pRowid);
    }
  else
    if (memcmp(pAux->pMod->xRowid_signature, xRowid_signatures[xRowid_cidxRowid_enum], sizeof(pAux->pMod->xRowid_signature)) == 0) {
      rc = cidxRowid(pCur->pChild, pRowid);
    }
  else
    if (memcmp(pAux->pMod->xRowid_signature, xRowid_signatures[xRowid_closureRowid_enum], sizeof(pAux->pMod->xRowid_signature)) == 0) {
      rc = closureRowid(pCur->pChild, pRowid);
    }
  else
    if (memcmp(pAux->pMod->xRowid_signature, xRowid_signatures[xRowid_completionRowid_enum], sizeof(pAux->pMod->xRowid_signature)) == 0) {
      rc = completionRowid(pCur->pChild, pRowid);
    }
  else
    if (memcmp(pAux->pMod->xRowid_signature, xRowid_signatures[xRowid_csvtabRowid_enum], sizeof(pAux->pMod->xRowid_signature)) == 0) {
      rc = csvtabRowid(pCur->pChild, pRowid);
    }
  else
    if (memcmp(pAux->pMod->xRowid_signature, xRowid_signatures[xRowid_dbdataRowid_enum], sizeof(pAux->pMod->xRowid_signature)) == 0) {
      rc = dbdataRowid(pCur->pChild, pRowid);
    }
  else
    if (memcmp(pAux->pMod->xRowid_signature, xRowid_signatures[xRowid_dbpageRowid_enum], sizeof(pAux->pMod->xRowid_signature)) == 0) {
      rc = dbpageRowid(pCur->pChild, pRowid);
    }
  else
    if (memcmp(pAux->pMod->xRowid_signature, xRowid_signatures[xRowid_deltaparsevtabRowid_enum], sizeof(pAux->pMod->xRowid_signature)) == 0) {
      rc = deltaparsevtabRowid(pCur->pChild, pRowid);
    }
  else
    if (memcmp(pAux->pMod->xRowid_signature, xRowid_signatures[xRowid_echoRowid_enum], sizeof(pAux->pMod->xRowid_signature)) == 0) {
      rc = echoRowid(pCur->pChild, pRowid);
    }
  else
    if (memcmp(pAux->pMod->xRowid_signature, xRowid_signatures[xRowid_expertRowid_enum], sizeof(pAux->pMod->xRowid_signature)) == 0) {
      rc = expertRowid(pCur->pChild, pRowid);
    }
  else
    if (memcmp(pAux->pMod->xRowid_signature, xRowid_signatures[xRowid_explainRowid_enum], sizeof(pAux->pMod->xRowid_signature)) == 0) {
      rc = explainRowid(pCur->pChild, pRowid);
    }
  else
    if (memcmp(pAux->pMod->xRowid_signature, xRowid_signatures[xRowid_fsRowid_enum], sizeof(pAux->pMod->xRowid_signature)) == 0) {
      rc = fsRowid(pCur->pChild, pRowid);
    }
  else
    if (memcmp(pAux->pMod->xRowid_signature, xRowid_signatures[xRowid_fsdirRowid_enum], sizeof(pAux->pMod->xRowid_signature)) == 0) {
      rc = fsdirRowid(pCur->pChild, pRowid);
    }
  else
    if (memcmp(pAux->pMod->xRowid_signature, xRowid_signatures[xRowid_fstreeRowid_enum], sizeof(pAux->pMod->xRowid_signature)) == 0) {
      rc = fstreeRowid(pCur->pChild, pRowid);
    }
  else
    if (memcmp(pAux->pMod->xRowid_signature, xRowid_signatures[xRowid_fts3RowidMethod_enum], sizeof(pAux->pMod->xRowid_signature)) == 0) {
      rc = fts3RowidMethod(pCur->pChild, pRowid);
    }
  else
    if (memcmp(pAux->pMod->xRowid_signature, xRowid_signatures[xRowid_fts3auxRowidMethod_enum], sizeof(pAux->pMod->xRowid_signature)) == 0) {
      rc = fts3auxRowidMethod(pCur->pChild, pRowid);
    }
  else
    if (memcmp(pAux->pMod->xRowid_signature, xRowid_signatures[xRowid_fts3termRowidMethod_enum], sizeof(pAux->pMod->xRowid_signature)) == 0) {
      rc = fts3termRowidMethod(pCur->pChild, pRowid);
    }
  else
    if (memcmp(pAux->pMod->xRowid_signature, xRowid_signatures[xRowid_fts3tokRowidMethod_enum], sizeof(pAux->pMod->xRowid_signature)) == 0) {
      rc = fts3tokRowidMethod(pCur->pChild, pRowid);
    }
  else
    if (memcmp(pAux->pMod->xRowid_signature, xRowid_signatures[xRowid_fuzzerRowid_enum], sizeof(pAux->pMod->xRowid_signature)) == 0) {
      rc = fuzzerRowid(pCur->pChild, pRowid);
    }
  else
    if (memcmp(pAux->pMod->xRowid_signature, xRowid_signatures[xRowid_intarrayRowid_enum], sizeof(pAux->pMod->xRowid_signature)) == 0) {
      rc = intarrayRowid(pCur->pChild, pRowid);
    }
  else
    if (memcmp(pAux->pMod->xRowid_signature, xRowid_signatures[xRowid_jsonEachRowid_enum], sizeof(pAux->pMod->xRowid_signature)) == 0) {
      rc = jsonEachRowid(pCur->pChild, pRowid);
    }
  else
    if (memcmp(pAux->pMod->xRowid_signature, xRowid_signatures[xRowid_memstatRowid_enum], sizeof(pAux->pMod->xRowid_signature)) == 0) {
      rc = memstatRowid(pCur->pChild, pRowid);
    }
  else
    if (memcmp(pAux->pMod->xRowid_signature, xRowid_signatures[xRowid_pragmaVtabRowid_enum], sizeof(pAux->pMod->xRowid_signature)) == 0) {
      rc = pragmaVtabRowid(pCur->pChild, pRowid);
    }
  else
    if (memcmp(pAux->pMod->xRowid_signature, xRowid_signatures[xRowid_prefixesRowid_enum], sizeof(pAux->pMod->xRowid_signature)) == 0) {
      rc = prefixesRowid(pCur->pChild, pRowid);
    }
  else
    if (memcmp(pAux->pMod->xRowid_signature, xRowid_signatures[xRowid_qpvtabRowid_enum], sizeof(pAux->pMod->xRowid_signature)) == 0) {
      rc = qpvtabRowid(pCur->pChild, pRowid);
    }
  else
    if (memcmp(pAux->pMod->xRowid_signature, xRowid_signatures[xRowid_rtreeRowid_enum], sizeof(pAux->pMod->xRowid_signature)) == 0) {
      rc = rtreeRowid(pCur->pChild, pRowid);
    }
  else
    if (memcmp(pAux->pMod->xRowid_signature, xRowid_signatures[xRowid_schemaRowid_enum], sizeof(pAux->pMod->xRowid_signature)) == 0) {
      rc = schemaRowid(pCur->pChild, pRowid);
    }
  else
    if (memcmp(pAux->pMod->xRowid_signature, xRowid_signatures[xRowid_seriesRowid_enum], sizeof(pAux->pMod->xRowid_signature)) == 0) {
      rc = seriesRowid(pCur->pChild, pRowid);
    }
  else
    if (memcmp(pAux->pMod->xRowid_signature, xRowid_signatures[xRowid_spellfix1Rowid_enum], sizeof(pAux->pMod->xRowid_signature)) == 0) {
      rc = spellfix1Rowid(pCur->pChild, pRowid);
    }
  else
    if (memcmp(pAux->pMod->xRowid_signature, xRowid_signatures[xRowid_statRowid_enum], sizeof(pAux->pMod->xRowid_signature)) == 0) {
      rc = statRowid(pCur->pChild, pRowid);
    }
  else
    if (memcmp(pAux->pMod->xRowid_signature, xRowid_signatures[xRowid_stmtRowid_enum], sizeof(pAux->pMod->xRowid_signature)) == 0) {
      rc = stmtRowid(pCur->pChild, pRowid);
    }
  else
    if (memcmp(pAux->pMod->xRowid_signature, xRowid_signatures[xRowid_tclRowid_enum], sizeof(pAux->pMod->xRowid_signature)) == 0) {
      rc = tclRowid(pCur->pChild, pRowid);
    }
  else
    if (memcmp(pAux->pMod->xRowid_signature, xRowid_signatures[xRowid_tclvarRowid_enum], sizeof(pAux->pMod->xRowid_signature)) == 0) {
      rc = tclvarRowid(pCur->pChild, pRowid);
    }
  else
    if (memcmp(pAux->pMod->xRowid_signature, xRowid_signatures[xRowid_templatevtabRowid_enum], sizeof(pAux->pMod->xRowid_signature)) == 0) {
      rc = templatevtabRowid(pCur->pChild, pRowid);
    }
  else
    if (memcmp(pAux->pMod->xRowid_signature, xRowid_signatures[xRowid_unionRowid_enum], sizeof(pAux->pMod->xRowid_signature)) == 0) {
      rc = unionRowid(pCur->pChild, pRowid);
    }
  else
    if (memcmp(pAux->pMod->xRowid_signature, xRowid_signatures[xRowid_vlogRowid_enum], sizeof(pAux->pMod->xRowid_signature)) == 0) {
      rc = vlogRowid(pCur->pChild, pRowid);
    }
  else
    if (memcmp(pAux->pMod->xRowid_signature, xRowid_signatures[xRowid_vstattabRowid_enum], sizeof(pAux->pMod->xRowid_signature)) == 0) {
      rc = vstattabRowid(pCur->pChild, pRowid);
    }
  else
    if (memcmp(pAux->pMod->xRowid_signature, xRowid_signatures[xRowid_vtablogRowid_enum], sizeof(pAux->pMod->xRowid_signature)) == 0) {
      rc = vtablogRowid(pCur->pChild, pRowid);
    }
  else
    if (memcmp(pAux->pMod->xRowid_signature, xRowid_signatures[xRowid_wholenumberRowid_enum], sizeof(pAux->pMod->xRowid_signature)) == 0) {
      rc = wholenumberRowid(pCur->pChild, pRowid);
    }
  if( rc!=SQLITE_OK ){
    VTSHIM_COPY_ERRMSG();
  }
  return rc;
}

static int vtshimUpdate(
  sqlite3_vtab *pBase,
  int argc,
  sqlite3_value **argv,
  sqlite3_int64 *pRowid
){
  vtshim_vtab *pVtab = (vtshim_vtab*)pBase;
  vtshim_aux *pAux = pVtab->pAux;
  int rc;
  if( pAux->bDisposed ) return SQLITE_ERROR;
  if (memcmp(pAux->pMod->xUpdate_signature, xUpdate_signatures[xUpdate_0_enum], sizeof(pAux->pMod->xUpdate_signature)) == 0) {
    rc = 0;
  }
  else
    if (memcmp(pAux->pMod->xUpdate_signature, xUpdate_signatures[xUpdate_amatchUpdate_enum], sizeof(pAux->pMod->xUpdate_signature)) == 0) {
      rc = amatchUpdate(pVtab->pChild, argc, argv, pRowid);
    }
  else
    if (memcmp(pAux->pMod->xUpdate_signature, xUpdate_signatures[xUpdate_csvtabUpdate_enum], sizeof(pAux->pMod->xUpdate_signature)) == 0) {
      rc = csvtabUpdate(pVtab->pChild, argc, argv, pRowid);
    }
  else
    if (memcmp(pAux->pMod->xUpdate_signature, xUpdate_signatures[xUpdate_dbpageUpdate_enum], sizeof(pAux->pMod->xUpdate_signature)) == 0) {
      rc = dbpageUpdate(pVtab->pChild, argc, argv, pRowid);
    }
  else
    if (memcmp(pAux->pMod->xUpdate_signature, xUpdate_signatures[xUpdate_echoUpdate_enum], sizeof(pAux->pMod->xUpdate_signature)) == 0) {
      rc = echoUpdate(pVtab->pChild, argc, argv, pRowid);
    }
  else
    if (memcmp(pAux->pMod->xUpdate_signature, xUpdate_signatures[xUpdate_expertUpdate_enum], sizeof(pAux->pMod->xUpdate_signature)) == 0) {
      rc = expertUpdate(pVtab->pChild, argc, argv, pRowid);
    }
  else
    if (memcmp(pAux->pMod->xUpdate_signature, xUpdate_signatures[xUpdate_fts3UpdateMethod_enum], sizeof(pAux->pMod->xUpdate_signature)) == 0) {
      rc = fts3UpdateMethod(pVtab->pChild, argc, argv, pRowid);
    }
  else
    if (memcmp(pAux->pMod->xUpdate_signature, xUpdate_signatures[xUpdate_geopolyUpdate_enum], sizeof(pAux->pMod->xUpdate_signature)) == 0) {
      rc = geopolyUpdate(pVtab->pChild, argc, argv, pRowid);
    }
  else
    if (memcmp(pAux->pMod->xUpdate_signature, xUpdate_signatures[xUpdate_rtreeUpdate_enum], sizeof(pAux->pMod->xUpdate_signature)) == 0) {
      rc = rtreeUpdate(pVtab->pChild, argc, argv, pRowid);
    }
  else
    if (memcmp(pAux->pMod->xUpdate_signature, xUpdate_signatures[xUpdate_spellfix1Update_enum], sizeof(pAux->pMod->xUpdate_signature)) == 0) {
      rc = spellfix1Update(pVtab->pChild, argc, argv, pRowid);
    }
  else
    if (memcmp(pAux->pMod->xUpdate_signature, xUpdate_signatures[xUpdate_tclvarUpdate_enum], sizeof(pAux->pMod->xUpdate_signature)) == 0) {
      rc = tclvarUpdate(pVtab->pChild, argc, argv, pRowid);
    }
  else
    if (memcmp(pAux->pMod->xUpdate_signature, xUpdate_signatures[xUpdate_vstattabUpdate_enum], sizeof(pAux->pMod->xUpdate_signature)) == 0) {
      rc = vstattabUpdate(pVtab->pChild, argc, argv, pRowid);
    }
  else
    if (memcmp(pAux->pMod->xUpdate_signature, xUpdate_signatures[xUpdate_vtablogUpdate_enum], sizeof(pAux->pMod->xUpdate_signature)) == 0) {
      rc = vtablogUpdate(pVtab->pChild, argc, argv, pRowid);
    }
  else
    if (memcmp(pAux->pMod->xUpdate_signature, xUpdate_signatures[xUpdate_zipfileUpdate_enum], sizeof(pAux->pMod->xUpdate_signature)) == 0) {
      rc = zipfileUpdate(pVtab->pChild, argc, argv, pRowid);
    }
  if( rc!=SQLITE_OK ){
    VTSHIM_COPY_ERRMSG();
  }
  return rc;
}

static int vtshimBegin(sqlite3_vtab *pBase){
  vtshim_vtab *pVtab = (vtshim_vtab*)pBase;
  vtshim_aux *pAux = pVtab->pAux;
  int rc;
  if( pAux->bDisposed ) return SQLITE_ERROR;
  if (memcmp(pAux->pMod->xBegin_signature, xBegin_signatures[xBegin_0_enum], sizeof(pAux->pMod->xBegin_signature)) == 0) {
    rc = 0;
  }
  else
    if (memcmp(pAux->pMod->xBegin_signature, xBegin_signatures[xBegin_dbpageBegin_enum], sizeof(pAux->pMod->xBegin_signature)) == 0) {
      rc = dbpageBegin(pVtab->pChild);
    }
  else
    if (memcmp(pAux->pMod->xBegin_signature, xBegin_signatures[xBegin_fts3BeginMethod_enum], sizeof(pAux->pMod->xBegin_signature)) == 0) {
      rc = fts3BeginMethod(pVtab->pChild);
    }
  else
    if (memcmp(pAux->pMod->xBegin_signature, xBegin_signatures[xBegin_rtreeBeginTransaction_enum], sizeof(pAux->pMod->xBegin_signature)) == 0) {
      rc = rtreeBeginTransaction(pVtab->pChild);
    }
  else
    if (memcmp(pAux->pMod->xBegin_signature, xBegin_signatures[xBegin_zipfileBegin_enum], sizeof(pAux->pMod->xBegin_signature)) == 0) {
      rc = zipfileBegin(pVtab->pChild);
    }
  if( rc!=SQLITE_OK ){
    VTSHIM_COPY_ERRMSG();
  }
  return rc;
}

static int vtshimSync(sqlite3_vtab *pBase){
  vtshim_vtab *pVtab = (vtshim_vtab*)pBase;
  vtshim_aux *pAux = pVtab->pAux;
  int rc;
  if( pAux->bDisposed ) return SQLITE_ERROR;
  if (memcmp(pAux->pMod->xSync_signature, xSync_signatures[xSync_0_enum], sizeof(pAux->pMod->xSync_signature)) == 0) {
    rc = 0;
  }
  else
    if (memcmp(pAux->pMod->xSync_signature, xSync_signatures[xSync_apndSync_enum], sizeof(pAux->pMod->xSync_signature)) == 0) {
      rc = apndSync(pVtab->pChild);
    }
  else
    if (memcmp(pAux->pMod->xSync_signature, xSync_signatures[xSync_dbpageSync_enum], sizeof(pAux->pMod->xSync_signature)) == 0) {
      rc = dbpageSync(pVtab->pChild);
    }
  else
    if (memcmp(pAux->pMod->xSync_signature, xSync_signatures[xSync_echoSync_enum], sizeof(pAux->pMod->xSync_signature)) == 0) {
      rc = echoSync(pVtab->pChild);
    }
  else
    if (memcmp(pAux->pMod->xSync_signature, xSync_signatures[xSync_fts3SyncMethod_enum], sizeof(pAux->pMod->xSync_signature)) == 0) {
      rc = fts3SyncMethod(pVtab->pChild);
    }
  else
    if (memcmp(pAux->pMod->xSync_signature, xSync_signatures[xSync_memdbSync_enum], sizeof(pAux->pMod->xSync_signature)) == 0) {
      rc = memdbSync(pVtab->pChild);
    }
  else
    if (memcmp(pAux->pMod->xSync_signature, xSync_signatures[xSync_memjrnlSync_enum], sizeof(pAux->pMod->xSync_signature)) == 0) {
      rc = memjrnlSync(pVtab->pChild);
    }
  else
    if (memcmp(pAux->pMod->xSync_signature, xSync_signatures[xSync_recoverVfsSync_enum], sizeof(pAux->pMod->xSync_signature)) == 0) {
      rc = recoverVfsSync(pVtab->pChild);
    }
  else
    if (memcmp(pAux->pMod->xSync_signature, xSync_signatures[xSync_rtreeEndTransaction_enum], sizeof(pAux->pMod->xSync_signature)) == 0) {
      rc = rtreeEndTransaction(pVtab->pChild);
    }
  else
    if (memcmp(pAux->pMod->xSync_signature, xSync_signatures[xSync_vfstraceSync_enum], sizeof(pAux->pMod->xSync_signature)) == 0) {
      rc = vfstraceSync(pVtab->pChild);
    }
  else
    if (memcmp(pAux->pMod->xSync_signature, xSync_signatures[xSync_vtablogSync_enum], sizeof(pAux->pMod->xSync_signature)) == 0) {
      rc = vtablogSync(pVtab->pChild);
    }
  else
    if (memcmp(pAux->pMod->xSync_signature, xSync_signatures[xSync_unixSync_enum], sizeof(pAux->pMod->xSync_signature)) == 0) {
      rc = unixSync(pVtab->pChild);
    }
  if( rc!=SQLITE_OK ){
    VTSHIM_COPY_ERRMSG();
  }
  return rc;
}

static int vtshimCommit(sqlite3_vtab *pBase){
  vtshim_vtab *pVtab = (vtshim_vtab*)pBase;
  vtshim_aux *pAux = pVtab->pAux;
  int rc;
  if( pAux->bDisposed ) return SQLITE_ERROR;
  if (memcmp(pAux->pMod->xCommit_signature, xCommit_signatures[xCommit_0_enum], sizeof(pAux->pMod->xCommit_signature)) == 0) {
    rc = 0;
  }
  else
    if (memcmp(pAux->pMod->xCommit_signature, xCommit_signatures[xCommit_echoCommit_enum], sizeof(pAux->pMod->xCommit_signature)) == 0) {
      rc = echoCommit(pVtab->pChild);
    }
  else
    if (memcmp(pAux->pMod->xCommit_signature, xCommit_signatures[xCommit_fts3CommitMethod_enum], sizeof(pAux->pMod->xCommit_signature)) == 0) {
      rc = fts3CommitMethod(pVtab->pChild);
    }
  else
    if (memcmp(pAux->pMod->xCommit_signature, xCommit_signatures[xCommit_rtreeEndTransaction_enum], sizeof(pAux->pMod->xCommit_signature)) == 0) {
      rc = rtreeEndTransaction(pVtab->pChild);
    }
  else
    if (memcmp(pAux->pMod->xCommit_signature, xCommit_signatures[xCommit_vtablogCommit_enum], sizeof(pAux->pMod->xCommit_signature)) == 0) {
      rc = vtablogCommit(pVtab->pChild);
    }
  else
    if (memcmp(pAux->pMod->xCommit_signature, xCommit_signatures[xCommit_zipfileCommit_enum], sizeof(pAux->pMod->xCommit_signature)) == 0) {
      rc = zipfileCommit(pVtab->pChild);
    }
  if( rc!=SQLITE_OK ){
    VTSHIM_COPY_ERRMSG();
  }
  return rc;
}

static int vtshimRollback(sqlite3_vtab *pBase){
  vtshim_vtab *pVtab = (vtshim_vtab*)pBase;
  vtshim_aux *pAux = pVtab->pAux;
  int rc;
  if( pAux->bDisposed ) return SQLITE_ERROR;
  if (memcmp(pAux->pMod->xRollback_signature, xRollback_signatures[xRollback_0_enum], sizeof(pAux->pMod->xRollback_signature)) == 0) {
    rc = 0;
  }
  else
    if (memcmp(pAux->pMod->xRollback_signature, xRollback_signatures[xRollback_echoRollback_enum], sizeof(pAux->pMod->xRollback_signature)) == 0) {
      rc = echoRollback(pVtab->pChild);
    }
  else
    if (memcmp(pAux->pMod->xRollback_signature, xRollback_signatures[xRollback_fts3RollbackMethod_enum], sizeof(pAux->pMod->xRollback_signature)) == 0) {
      rc = fts3RollbackMethod(pVtab->pChild);
    }
  else
    if (memcmp(pAux->pMod->xRollback_signature, xRollback_signatures[xRollback_rtreeEndTransaction_enum], sizeof(pAux->pMod->xRollback_signature)) == 0) {
      rc = rtreeEndTransaction(pVtab->pChild);
    }
  else
    if (memcmp(pAux->pMod->xRollback_signature, xRollback_signatures[xRollback_rtreeRollback_enum], sizeof(pAux->pMod->xRollback_signature)) == 0) {
      rc = rtreeRollback(pVtab->pChild);
    }
  else
    if (memcmp(pAux->pMod->xRollback_signature, xRollback_signatures[xRollback_vtablogRollback_enum], sizeof(pAux->pMod->xRollback_signature)) == 0) {
      rc = vtablogRollback(pVtab->pChild);
    }
  else
    if (memcmp(pAux->pMod->xRollback_signature, xRollback_signatures[xRollback_zipfileRollback_enum], sizeof(pAux->pMod->xRollback_signature)) == 0) {
      rc = zipfileRollback(pVtab->pChild);
    }
  if( rc!=SQLITE_OK ){
    VTSHIM_COPY_ERRMSG();
  }
  return rc;
}

static int vtshimFindFunction(
  sqlite3_vtab *pBase,
  int nArg,
  const char *zName,
  void (**pxFunc)(sqlite3_context*,int,sqlite3_value**),
  void **ppArg
){
  vtshim_vtab *pVtab = (vtshim_vtab*)pBase;
  vtshim_aux *pAux = pVtab->pAux;
  int rc;
  if( pAux->bDisposed ) return 0;
  if (memcmp(pAux->pMod->xFindFunction_signature, xFindFunction_signatures[xFindFunction_0_enum], sizeof(pAux->pMod->xFindFunction_signature)) == 0) {
    rc = 0;
  }
  else
    if (memcmp(pAux->pMod->xFindFunction_signature, xFindFunction_signatures[xFindFunction_fts3FindFunctionMethod_enum], sizeof(pAux->pMod->xFindFunction_signature)) == 0) {
      rc = fts3FindFunctionMethod(pVtab->pChild, nArg, zName, pxFunc, ppArg);
    }
  else
    if (memcmp(pAux->pMod->xFindFunction_signature, xFindFunction_signatures[xFindFunction_zipfileFindFunction_enum], sizeof(pAux->pMod->xFindFunction_signature)) == 0) {
      rc = zipfileFindFunction(pVtab->pChild, nArg, zName, pxFunc, ppArg);
    }
  VTSHIM_COPY_ERRMSG();
  return rc;
}

static int vtshimRename(sqlite3_vtab *pBase, const char *zNewName){
  vtshim_vtab *pVtab = (vtshim_vtab*)pBase;
  vtshim_aux *pAux = pVtab->pAux;
  int rc;
  if( pAux->bDisposed ) return SQLITE_ERROR;
  if (memcmp(pAux->pMod->xRename_signature, xRename_signatures[xRename_0_enum], sizeof(pAux->pMod->xRename_signature)) == 0) {
    rc = 0;
  }
  else
    if (memcmp(pAux->pMod->xRename_signature, xRename_signatures[xRename_echoRename_enum], sizeof(pAux->pMod->xRename_signature)) == 0) {
      rc = echoRename(pVtab->pChild, zNewName);
    }
  else
    if (memcmp(pAux->pMod->xRename_signature, xRename_signatures[xRename_fts3RenameMethod_enum], sizeof(pAux->pMod->xRename_signature)) == 0) {
      rc = fts3RenameMethod(pVtab->pChild, zNewName);
    }
  else
    if (memcmp(pAux->pMod->xRename_signature, xRename_signatures[xRename_rtreeRename_enum], sizeof(pAux->pMod->xRename_signature)) == 0) {
      rc = rtreeRename(pVtab->pChild, zNewName);
    }
  else
    if (memcmp(pAux->pMod->xRename_signature, xRename_signatures[xRename_spellfix1Rename_enum], sizeof(pAux->pMod->xRename_signature)) == 0) {
      rc = spellfix1Rename(pVtab->pChild, zNewName);
    }
  else
    if (memcmp(pAux->pMod->xRename_signature, xRename_signatures[xRename_vtablogRename_enum], sizeof(pAux->pMod->xRename_signature)) == 0) {
      rc = vtablogRename(pVtab->pChild, zNewName);
    }
  if( rc!=SQLITE_OK ){
    VTSHIM_COPY_ERRMSG();
  }
  return rc;
}

static int vtshimSavepoint(sqlite3_vtab *pBase, int n){
  vtshim_vtab *pVtab = (vtshim_vtab*)pBase;
  vtshim_aux *pAux = pVtab->pAux;
  int rc;
  if( pAux->bDisposed ) return SQLITE_ERROR;
  if (memcmp(pAux->pMod->xSavepoint_signature, xSavepoint_signatures[xSavepoint_0_enum], sizeof(pAux->pMod->xSavepoint_signature)) == 0) {
    rc = 0;
  }
  else
    if (memcmp(pAux->pMod->xSavepoint_signature, xSavepoint_signatures[xSavepoint_fts3SavepointMethod_enum], sizeof(pAux->pMod->xSavepoint_signature)) == 0) {
      rc = fts3SavepointMethod(pVtab->pChild, n);
    }
  else
    if (memcmp(pAux->pMod->xSavepoint_signature, xSavepoint_signatures[xSavepoint_rtreeSavepoint_enum], sizeof(pAux->pMod->xSavepoint_signature)) == 0) {
      rc = rtreeSavepoint(pVtab->pChild, n);
    }
  if( rc!=SQLITE_OK ){
    VTSHIM_COPY_ERRMSG();
  }
  return rc;
}

static int vtshimRelease(sqlite3_vtab *pBase, int n){
  vtshim_vtab *pVtab = (vtshim_vtab*)pBase;
  vtshim_aux *pAux = pVtab->pAux;
  int rc;
  if( pAux->bDisposed ) return SQLITE_ERROR;
  if (memcmp(pAux->pMod->xRelease_signature, xRelease_signatures[xRelease_0_enum], sizeof(pAux->pMod->xRelease_signature)) == 0) {
    rc = 0;
  }
  else
    if (memcmp(pAux->pMod->xRelease_signature, xRelease_signatures[xRelease_echoRelease_enum], sizeof(pAux->pMod->xRelease_signature)) == 0) {
      rc = echoRelease(pVtab->pChild, n);
    }
  else
    if (memcmp(pAux->pMod->xRelease_signature, xRelease_signatures[xRelease_fts3ReleaseMethod_enum], sizeof(pAux->pMod->xRelease_signature)) == 0) {
      rc = fts3ReleaseMethod(pVtab->pChild, n);
    }
  else
    if (memcmp(pAux->pMod->xRelease_signature, xRelease_signatures[xRelease_vtablogRelease_enum], sizeof(pAux->pMod->xRelease_signature)) == 0) {
      rc = vtablogRelease(pVtab->pChild, n);
    }
  if( rc!=SQLITE_OK ){
    VTSHIM_COPY_ERRMSG();
  }
  return rc;
}

static int vtshimRollbackTo(sqlite3_vtab *pBase, int n){
  vtshim_vtab *pVtab = (vtshim_vtab*)pBase;
  vtshim_aux *pAux = pVtab->pAux;
  int rc;
  if( pAux->bDisposed ) return SQLITE_ERROR;
  if (memcmp(pAux->pMod->xRollbackTo_signature, xRollbackTo_signatures[xRollbackTo_0_enum], sizeof(pAux->pMod->xRollbackTo_signature)) == 0) {
    rc = 0;
  }
  else
    if (memcmp(pAux->pMod->xRollbackTo_signature, xRollbackTo_signatures[xRollbackTo_dbpageRollbackTo_enum], sizeof(pAux->pMod->xRollbackTo_signature)) == 0) {
      rc = dbpageRollbackTo(pVtab->pChild, n);
    }
  else
    if (memcmp(pAux->pMod->xRollbackTo_signature, xRollbackTo_signatures[xRollbackTo_echoRollbackTo_enum], sizeof(pAux->pMod->xRollbackTo_signature)) == 0) {
      rc = echoRollbackTo(pVtab->pChild, n);
    }
  else
    if (memcmp(pAux->pMod->xRollbackTo_signature, xRollbackTo_signatures[xRollbackTo_fts3RollbackToMethod_enum], sizeof(pAux->pMod->xRollbackTo_signature)) == 0) {
      rc = fts3RollbackToMethod(pVtab->pChild, n);
    }
  else
    if (memcmp(pAux->pMod->xRollbackTo_signature, xRollbackTo_signatures[xRollbackTo_vtablogRollbackTo_enum], sizeof(pAux->pMod->xRollbackTo_signature)) == 0) {
      rc = vtablogRollbackTo(pVtab->pChild, n);
    }
  if( rc!=SQLITE_OK ){
    VTSHIM_COPY_ERRMSG();
  }
  return rc;
}

/* The destructor function for a disposable module */
static void vtshimAuxDestructor(void *pXAux){
  vtshim_aux *pAux = (vtshim_aux*)pXAux;
  assert( pAux->pAllVtab==0 );
  if( !pAux->bDisposed && pAux->xChildDestroy ){
    pAux->xChildDestroy(pAux->pChildAux);
    pAux->xChildDestroy = 0;
  }
  sqlite3_free(pAux->zName);
  sqlite3_free(pAux->pMod);
  sqlite3_free(pAux);
}

static int vtshimCopyModule(
  const sqlite3_module *pMod,   /* Source module to be copied */
  sqlite3_module **ppMod        /* Destination for copied module */
){
  sqlite3_module *p;
  if( !pMod || !ppMod ) return SQLITE_ERROR;
  p = sqlite3_malloc( sizeof(*p) );
  if( p==0 ) return SQLITE_NOMEM;
  memcpy(p, pMod, sizeof(*p));
  *ppMod = p;
  return SQLITE_OK;
}

#ifdef _WIN32
__declspec(dllexport)
#endif
void *sqlite3_create_disposable_module(
  sqlite3 *db,               /* SQLite connection to register module with */
  const char *zName,         /* Name of the module */
  const sqlite3_module *p,   /* Methods for the module */
  void *pClientData,         /* Client data for xCreate/xConnect */
  void(*xDestroy)(void*)     /* Module destructor function */
){
  vtshim_aux *pAux;
  sqlite3_module *pMod;
  int rc;
  pAux = sqlite3_malloc( sizeof(*pAux) );
  if( pAux==0 ){
    if( xDestroy ) xDestroy(pClientData);
    return 0;
  }
  rc = vtshimCopyModule(p, &pMod);
  if( rc!=SQLITE_OK ){
    sqlite3_free(pAux);
    return 0;
  }
  pAux->pChildAux = pClientData;
  pAux->xChildDestroy = xDestroy;
  pAux->pMod = pMod;
  pAux->db = db;
  pAux->zName = sqlite3_mprintf("%s", zName);
  pAux->bDisposed = 0;
  pAux->pAllVtab = 0;
  pAux->sSelf.iVersion = p->iVersion<=2 ? p->iVersion : 2;
  pAux->sSelf.xCreate = p->xCreate ? vtshimCreate : 0;
  pAux->sSelf.xConnect = p->xConnect ? vtshimConnect : 0;
  pAux->sSelf.xBestIndex = p->xBestIndex ? vtshimBestIndex : 0;
  pAux->sSelf.xDisconnect = p->xDisconnect ? vtshimDisconnect : 0;
  pAux->sSelf.xDestroy = p->xDestroy ? vtshimDestroy : 0;
  pAux->sSelf.xOpen = p->xOpen ? vtshimOpen : 0;
  pAux->sSelf.xOpen_signature = p->xOpen_signature ? xOpen_signatures[xOpen_vtshimOpen_enum] : 0;

  pAux->sSelf.xClose = p->xClose ? vtshimClose : 0;
  pAux->sSelf.xClose_signature = p->xClose_signature ? xClose_signatures[xClose_vtshimClose_enum] : 0;

  pAux->sSelf.xFilter = p->xFilter ? vtshimFilter : 0;
  pAux->sSelf.xNext = p->xNext ? vtshimNext : 0;
  pAux->sSelf.xEof = p->xEof ? vtshimEof : 0;
  pAux->sSelf.xColumn = p->xColumn ? vtshimColumn : 0;
  pAux->sSelf.xRowid = p->xRowid ? vtshimRowid : 0;
  pAux->sSelf.xUpdate = p->xUpdate ? vtshimUpdate : 0;
  pAux->sSelf.xBegin = p->xBegin ? vtshimBegin : 0;
  pAux->sSelf.xSync = p->xSync ? vtshimSync : 0;
  pAux->sSelf.xCommit = p->xCommit ? vtshimCommit : 0;
  pAux->sSelf.xRollback = p->xRollback ? vtshimRollback : 0;
  pAux->sSelf.xFindFunction = p->xFindFunction ? vtshimFindFunction : 0;
  pAux->sSelf.xRename = p->xRename ? vtshimRename : 0;
  if( p->iVersion>=2 ){
    pAux->sSelf.xSavepoint = p->xSavepoint ? vtshimSavepoint : 0;
    pAux->sSelf.xRelease = p->xRelease ? vtshimRelease : 0;
    pAux->sSelf.xRollbackTo = p->xRollbackTo ? vtshimRollbackTo : 0;
  }else{
    pAux->sSelf.xSavepoint = 0;
    pAux->sSelf.xSavepoint_signature = xSavepoint_signatures[xSavepoint_0_enum];
    pAux->sSelf.xRelease = 0;
    pAux->sSelf.xRelease_signature = xRelease_signatures[xRelease_0_enum];
    pAux->sSelf.xRollbackTo = 0;
    pAux->sSelf.xRollbackTo_signature = xRollbackTo_signatures[xRollbackTo_0_enum];
  }
  rc = sqlite3_create_module_v2(db, zName, &pAux->sSelf,
                                pAux, vtshimAuxDestructor);
  return rc==SQLITE_OK ? (void*)pAux : 0;
}

#ifdef _WIN32
__declspec(dllexport)
#endif
void sqlite3_dispose_module(void *pX){
  vtshim_aux *pAux = (vtshim_aux*)pX;
  if( !pAux->bDisposed ){
    vtshim_vtab *pVtab;
    vtshim_cursor *pCur;
    for(pVtab=pAux->pAllVtab; pVtab; pVtab=pVtab->pNext){
      for(pCur=pVtab->pAllCur; pCur; pCur=pCur->pNext){
        if (memcmp(pAux->pMod->xClose_signature, xClose_signatures[xClose_apndClose_enum], sizeof(pAux->pMod->xClose_signature)) == 0) {
          apndClose(pCur->pChild);
        }
        else
          if (memcmp(pAux->pMod->xClose_signature, xClose_signatures[xClose_bytecodevtabClose_enum], sizeof(pAux->pMod->xClose_signature)) == 0) {
            bytecodevtabClose(pCur->pChild);
          }
        else
          if (memcmp(pAux->pMod->xClose_signature, xClose_signatures[xClose_completionClose_enum], sizeof(pAux->pMod->xClose_signature)) == 0) {
            completionClose(pCur->pChild);
          }
        else
          if (memcmp(pAux->pMod->xClose_signature, xClose_signatures[xClose_dbdataClose_enum], sizeof(pAux->pMod->xClose_signature)) == 0) {
            dbdataClose(pCur->pChild);
          }
        else
          if (memcmp(pAux->pMod->xClose_signature, xClose_signatures[xClose_dbpageClose_enum], sizeof(pAux->pMod->xClose_signature)) == 0) {
            dbpageClose(pCur->pChild);
          }
        else
          if (memcmp(pAux->pMod->xClose_signature, xClose_signatures[xClose_expertClose_enum], sizeof(pAux->pMod->xClose_signature)) == 0) {
            expertClose(pCur->pChild);
          }
        else
          if (memcmp(pAux->pMod->xClose_signature, xClose_signatures[xClose_fsdirClose_enum], sizeof(pAux->pMod->xClose_signature)) == 0) {
            fsdirClose(pCur->pChild);
          }
        else
          if (memcmp(pAux->pMod->xClose_signature, xClose_signatures[xClose_fts3CloseMethod_enum], sizeof(pAux->pMod->xClose_signature)) == 0) {
            fts3CloseMethod(pCur->pChild);
          }
        else
          if (memcmp(pAux->pMod->xClose_signature, xClose_signatures[xClose_fts3auxCloseMethod_enum], sizeof(pAux->pMod->xClose_signature)) == 0) {
            fts3auxCloseMethod(pCur->pChild);
          }
        else
          if (memcmp(pAux->pMod->xClose_signature, xClose_signatures[xClose_fts3tokCloseMethod_enum], sizeof(pAux->pMod->xClose_signature)) == 0) {
            fts3tokCloseMethod(pCur->pChild);
          }
        else
          if (memcmp(pAux->pMod->xClose_signature, xClose_signatures[xClose_jsonEachClose_enum], sizeof(pAux->pMod->xClose_signature)) == 0) {
            jsonEachClose(pCur->pChild);
          }
        else
          if (memcmp(pAux->pMod->xClose_signature, xClose_signatures[xClose_memdbClose_enum], sizeof(pAux->pMod->xClose_signature)) == 0) {
            memdbClose(pCur->pChild);
          }
        else
          if (memcmp(pAux->pMod->xClose_signature, xClose_signatures[xClose_memjrnlClose_enum], sizeof(pAux->pMod->xClose_signature)) == 0) {
            memjrnlClose(pCur->pChild);
          }
        else
          if (memcmp(pAux->pMod->xClose_signature, xClose_signatures[xClose_porterClose_enum], sizeof(pAux->pMod->xClose_signature)) == 0) {
            porterClose(pCur->pChild);
          }
        else
          if (memcmp(pAux->pMod->xClose_signature, xClose_signatures[xClose_pragmaVtabClose_enum], sizeof(pAux->pMod->xClose_signature)) == 0) {
            pragmaVtabClose(pCur->pChild);
          }
        else
          if (memcmp(pAux->pMod->xClose_signature, xClose_signatures[xClose_recoverVfsClose_enum], sizeof(pAux->pMod->xClose_signature)) == 0) {
            recoverVfsClose(pCur->pChild);
          }
        else
          if (memcmp(pAux->pMod->xClose_signature, xClose_signatures[xClose_rtreeClose_enum], sizeof(pAux->pMod->xClose_signature)) == 0) {
            rtreeClose(pCur->pChild);
          }
        else
          if (memcmp(pAux->pMod->xClose_signature, xClose_signatures[xClose_seriesClose_enum], sizeof(pAux->pMod->xClose_signature)) == 0) {
            seriesClose(pCur->pChild);
          }
        else
          if (memcmp(pAux->pMod->xClose_signature, xClose_signatures[xClose_simpleClose_enum], sizeof(pAux->pMod->xClose_signature)) == 0) {
            simpleClose(pCur->pChild);
          }
        else
          if (memcmp(pAux->pMod->xClose_signature, xClose_signatures[xClose_statClose_enum], sizeof(pAux->pMod->xClose_signature)) == 0) {
            statClose(pCur->pChild);
          }
        else
          if (memcmp(pAux->pMod->xClose_signature, xClose_signatures[xClose_stmtClose_enum], sizeof(pAux->pMod->xClose_signature)) == 0) {
            stmtClose(pCur->pChild);
          }
        else
          if (memcmp(pAux->pMod->xClose_signature, xClose_signatures[xClose_unicodeClose_enum], sizeof(pAux->pMod->xClose_signature)) == 0) {
            unicodeClose(pCur->pChild);
          }
        else
          if (memcmp(pAux->pMod->xClose_signature, xClose_signatures[xClose_vfstraceClose_enum], sizeof(pAux->pMod->xClose_signature)) == 0) {
            vfstraceClose(pCur->pChild);
          }
        else
          if (memcmp(pAux->pMod->xClose_signature, xClose_signatures[xClose_zipfileClose_enum], sizeof(pAux->pMod->xClose_signature)) == 0) {
            zipfileClose(pCur->pChild);
          }
        else
          if (memcmp(pAux->pMod->xClose_signature, xClose_signatures[xClose_unixClose_enum], sizeof(pAux->pMod->xClose_signature)) == 0) {
            unixClose(pCur->pChild);
          }
        else
          if (memcmp(pAux->pMod->xClose_signature, xClose_signatures[xClose_nolockClose_enum], sizeof(pAux->pMod->xClose_signature)) == 0) {
            nolockClose(pCur->pChild);
          }
        else
          if (memcmp(pAux->pMod->xClose_signature, xClose_signatures[xClose_dotlockClose_enum], sizeof(pAux->pMod->xClose_signature)) == 0) {
            dotlockClose(pCur->pChild);
          }
      }
      if (memcmp(pAux->pMod->xDisconnect_signature, xDisconnect_signatures[xDisconnect_bytecodevtabDisconnect_enum], sizeof(pAux->pMod->xDisconnect_signature)) == 0) {
        bytecodevtabDisconnect(pVtab->pChild);
      }
      else
        if (memcmp(pAux->pMod->xDisconnect_signature, xDisconnect_signatures[xDisconnect_completionDisconnect_enum], sizeof(pAux->pMod->xDisconnect_signature)) == 0) {
          completionDisconnect(pVtab->pChild);
        }
      else
        if (memcmp(pAux->pMod->xDisconnect_signature, xDisconnect_signatures[xDisconnect_dbdataDisconnect_enum], sizeof(pAux->pMod->xDisconnect_signature)) == 0) {
          dbdataDisconnect(pVtab->pChild);
        }
      else
        if (memcmp(pAux->pMod->xDisconnect_signature, xDisconnect_signatures[xDisconnect_dbpageDisconnect_enum], sizeof(pAux->pMod->xDisconnect_signature)) == 0) {
          dbpageDisconnect(pVtab->pChild);
        }
      else
        if (memcmp(pAux->pMod->xDisconnect_signature, xDisconnect_signatures[xDisconnect_expertDisconnect_enum], sizeof(pAux->pMod->xDisconnect_signature)) == 0) {
          expertDisconnect(pVtab->pChild);
        }
      else
        if (memcmp(pAux->pMod->xDisconnect_signature, xDisconnect_signatures[xDisconnect_fsdirDisconnect_enum], sizeof(pAux->pMod->xDisconnect_signature)) == 0) {
          fsdirDisconnect(pVtab->pChild);
        }
      else
        if (memcmp(pAux->pMod->xDisconnect_signature, xDisconnect_signatures[xDisconnect_fts3DisconnectMethod_enum], sizeof(pAux->pMod->xDisconnect_signature)) == 0) {
          fts3DisconnectMethod(pVtab->pChild);
        }
      else
        if (memcmp(pAux->pMod->xDisconnect_signature, xDisconnect_signatures[xDisconnect_fts3auxDisconnectMethod_enum], sizeof(pAux->pMod->xDisconnect_signature)) == 0) {
          fts3auxDisconnectMethod(pVtab->pChild);
        }
      else
        if (memcmp(pAux->pMod->xDisconnect_signature, xDisconnect_signatures[xDisconnect_fts3tokDisconnectMethod_enum], sizeof(pAux->pMod->xDisconnect_signature)) == 0) {
          fts3tokDisconnectMethod(pVtab->pChild);
        }
      else
        if (memcmp(pAux->pMod->xDisconnect_signature, xDisconnect_signatures[xDisconnect_jsonEachDisconnect_enum], sizeof(pAux->pMod->xDisconnect_signature)) == 0) {
          jsonEachDisconnect(pVtab->pChild);
        }
      else
        if (memcmp(pAux->pMod->xDisconnect_signature, xDisconnect_signatures[xDisconnect_pragmaVtabDisconnect_enum], sizeof(pAux->pMod->xDisconnect_signature)) == 0) {
          pragmaVtabDisconnect(pVtab->pChild);
        }
      else
        if (memcmp(pAux->pMod->xDisconnect_signature, xDisconnect_signatures[xDisconnect_rtreeDisconnect_enum], sizeof(pAux->pMod->xDisconnect_signature)) == 0) {
          rtreeDisconnect(pVtab->pChild);
        }
      else
        if (memcmp(pAux->pMod->xDisconnect_signature, xDisconnect_signatures[xDisconnect_seriesDisconnect_enum], sizeof(pAux->pMod->xDisconnect_signature)) == 0) {
          seriesDisconnect(pVtab->pChild);
        }
      else
        if (memcmp(pAux->pMod->xDisconnect_signature, xDisconnect_signatures[xDisconnect_statDisconnect_enum], sizeof(pAux->pMod->xDisconnect_signature)) == 0) {
          statDisconnect(pVtab->pChild);
        }
      else
        if (memcmp(pAux->pMod->xDisconnect_signature, xDisconnect_signatures[xDisconnect_stmtDisconnect_enum], sizeof(pAux->pMod->xDisconnect_signature)) == 0) {
          stmtDisconnect(pVtab->pChild);
        }
      else
        if (memcmp(pAux->pMod->xDisconnect_signature, xDisconnect_signatures[xDisconnect_zipfileDisconnect_enum], sizeof(pAux->pMod->xDisconnect_signature)) == 0) {
          zipfileDisconnect(pVtab->pChild);
        }
    }
    pAux->bDisposed = 1;
    if( pAux->xChildDestroy ){
      pAux->xChildDestroy(pAux->pChildAux);
      pAux->xChildDestroy = 0;
    }
  }
}


#endif /* SQLITE_OMIT_VIRTUALTABLE */

#ifdef _WIN32
__declspec(dllexport)
#endif
int sqlite3_vtshim_init(
  sqlite3 *db,
  char **pzErrMsg,
  const sqlite3_api_routines *pApi
){
  SQLITE_EXTENSION_INIT2(pApi);
  return SQLITE_OK;
}
